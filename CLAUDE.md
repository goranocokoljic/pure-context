If you ever need to run python scripts, use python, not python 3.

# PureContext MCP — Project Instructions

## What This Project Is

PureContext MCP is a Node.js/TypeScript MCP (Model Context Protocol) code-intelligence server for AI agents. It indexes codebases using tree-sitter AST parsing, stores structured symbol metadata in SQLite, and serves two layers: **token-efficient retrieval** (let agents pull the exact symbols they need instead of reading whole files — the original, benchmarked foundation) and **change intelligence** (blast radius, temporal co-change, composite per-symbol risk, and refactor-safety checks so an agent can assess the impact and risk of an edit before making it). Retrieval is the foundation; safe autonomous change is the differentiation.

The full product requirements are in `docs/PureContext_MCP_PRD_v1.0.docx`. Read it before making architectural decisions.

---

## Architecture Rules

### Three-Layer Architecture (never violate)

1. **Core** (`src/core/`) — File discovery, content hashing, tree-sitter dispatch, SQLite storage, MCP transport, file watcher. Knows nothing about specific languages or frameworks.
2. **Language Handlers** (`src/handlers/`) — Map file extensions to tree-sitter grammars, define which AST node types are symbols, extract signatures and imports. Each handler is a self-contained module implementing `LanguageHandler`.
3. **Framework Adapters** (`src/adapters/`) — Domain-specific symbol extraction on top of language handlers. Auto-detected from project config files. Each adapter implements `FrameworkAdapter`. Optional and composable.

**The dependency direction is strictly downward: Adapters → Handlers → Core. Never the reverse.**

### Key Interfaces

```typescript
// src/core/types.ts

interface SymbolRecord {
  id: string;                    // Deterministic hash: SHA-256(filePath:name:kind).slice(0,16)
  name: string;
  kind: SymbolKind;
  filePath: string;              // Relative to repo root
  startByte: number;
  endByte: number;
  signature: string;             // One-line signature
  summary: string;               // One-line description
  frameworkMeta?: Record<string, unknown>;
}

type SymbolKind =
  | 'function' | 'class' | 'method' | 'const' | 'type'
  | 'interface' | 'enum' | 'component' | 'composable'
  | 'hook' | 'route' | 'decorator' | 'middleware' | 'property';

interface LanguageHandler {
  extensions(): string[];
  grammarPath(): string;         // Path to .wasm file (null for regex-only handlers)
  extractSymbols(tree: Tree, source: Buffer): SymbolRecord[];
  extractImports(tree: Tree, source: Buffer): ImportRecord[];
  extractDocstring(node: SyntaxNode): string | null;
}

interface FrameworkAdapter {
  name: string;
  detect(projectRoot: string): Promise<boolean>;
  fileFilter(filePath: string): boolean;
  preProcess?(source: Buffer, filePath: string): ProcessedBlock[];
  extractFrameworkSymbols(tree: Tree, source: Buffer, filePath: string): SymbolRecord[];
  enrichMetadata?(symbol: SymbolRecord): SymbolRecord;
}
```

### Database

- Use `better-sqlite3` (synchronous, fast)
- Single SQLite file per indexed project, stored in `~/.purecontext/indexes/`
- Four tables: `symbols`, `files`, `dep_edges`, `repos`
- Deterministic repo IDs: `SHA-256(absolutePath).slice(0, 16)`

### Tree-sitter

- Use `web-tree-sitter` (WASM bindings) — no native compilation
- Bundle `.wasm` grammar files in `grammars/` directory
- Parse dispatcher in core receives a file, resolves the handler, calls tree-sitter
- Regex-only handlers (SCSS, LESS, CSS) set `grammarPath()` to return `null`

### MCP Server

- Use `@modelcontextprotocol/sdk` for protocol handling
- Each tool is a separate file in `src/server/tools/`
- Tool handler receives parsed input, calls core services, returns structured response
- SDK >=1.29 requires async/await + try-catch in `typed()` — never `.catch()` on handler results

---

## Coding Conventions

- **Language**: TypeScript with strict mode; ES modules (`"type": "module"`)
- **Node.js**: >= 18.0.0
- **No classes unless necessary** — prefer functions and plain objects. Use classes only for stateful services (IndexManager, Watcher) where lifecycle matters.
- **Error handling**: Typed error classes extending `PureContextError`. Never swallow errors silently.
- **Logging**: Leveled logger (debug/info/warn/error). No `console.log` in production code.

**Naming:**
- Files: `kebab-case.ts`
- Types/interfaces: `PascalCase`
- Functions/variables: `camelCase`
- Constants: `UPPER_SNAKE_CASE`
- Database columns: `snake_case`

**Testing:**
- Use `vitest`
- Test directory mirrors src: `test/core/`, `test/handlers/`, `test/adapters/`, `test/server/`
- Integration tests use fixture projects in `test/fixtures/`
- Every language handler and framework adapter must have tests against real AST output

**Key directories:**
```
src/core/          # Indexing pipeline, SQLite, file watcher
src/handlers/      # Language handlers (one file per language)
src/adapters/      # Framework adapters (Vue, React, etc.)
src/server/tools/  # One file per MCP tool
src/graph/         # Dependency graph traversal
src/summarizer/    # Symbol summarization (docstring → AI → signature fallback)
src/config/        # Config loading and validation
grammars/          # Bundled .wasm tree-sitter grammar files
test/              # Mirrors src/; fixtures in test/fixtures/
scripts/hooks/     # Claude Code hook scripts (Node.js, cross-platform)
dev-docs/          # Phase task files, benchmark notes (gitignored, not public)
```

---

## Current Phase

**Latest: Phase 91 — COMPLETE** (Index Durability at Scale + Install UX — install-runbook findings)

Trigger: the reporter's install runbook (`dev-docs/purecontext-install-runbook.md`) — 89,187-file tree ran 110 min committing ZERO rows (one unbounded transaction, 4KB main .db + 444MB WAL, kill = total loss), installer "is a trap" (7 hooks, per-edit stderr reminder, ~3KB absolutist instruction block), `.gitignore` beat user excludePatterns, Node 26 forced a native compile. (1) **Task 563 chunked commits:** the single outer transaction in `index-manager` replaced by per-batch transactions (`indexing.commitBatchSize` config + `IndexOptions.commitBatchSize` override, default 500; 0 = pre-91 single transaction) with `PRAGMA wal_checkpoint(PASSIVE)` per batch (try/catch — WASM tier tolerated); PARALLEL path runs one `pool.run` per batch (peak memory = one batch of parse results, not the whole repo), SEQUENTIAL path parses async per chunk then writes one sync transaction (better-sqlite3 transactions can't await); shared `commitBatch` closure = one write-body for both paths; kill+resume works because upsertFile commits per batch and the hash cache skips committed files; additive `batchesCommitted` on IndexResult + tool response. (2) **Task 565 exclusion precedence:** `buildIgnoreFilter` order now built-ins → `.gitignore` → USER patterns (later wins negations in the `ignore` package, so `!protected/` in config rescues a nested repo the root .gitignore hides — previously impossible); honesty pre-pass reports TOP-LEVEL dirs entirely dropped with rule source (`excludedDirs: [{dir, source: builtin|gitignore|config}]` — builtin drops filtered out of the tool response as expected noise); RabbitMQ trailing-slash negation tests stay green. (3) **Task 564 installer diet:** all four questioned hook events VERIFIED against current Claude Code docs (WorktreeCreate/WorktreeRemove/TaskCompleted/SubagentStart all exist — none deleted); PreToolUse edit reminder now opt-in (`hooks --install --with-reminders`; default run REMOVES a previously opted-in reminder so re-runs converge; foreign PreToolUse hooks never touched); `install claude`/`install all` default = registration + instruction block only, hooks behind `--with-hooks` (global scope still injects ~/.claude/CLAUDE.md via `injectClaudeMd`); print-before-write; `assets/agent-rules.md` rewritten as defaults-with-exceptions (~half size, "Prefer…" not "Never…", gate loop scoped to non-trivial changes) + `npm run sync:rules`. (4) **Task 566 clean install on current Node:** `better-sqlite3` → **optionalDependencies** (install succeeds without a toolchain; Phase-78 WASM tier takes over at runtime), prebuild targets extended (+node@24/26 — matrix claim in docs kept honest: upstream-dependent), postinstall canary message now says "NOT fatal, WASM fallback"; `config --check` made async and reports the ACTIVE tier via `initSqliteBackend()` (was: a sync native-only probe that couldn't see the WASM tier). (5) **Task 567 runbook → docs:** new `docs/28-operations.md` (scoped-index guidance + corpus-hygiene numbers 56.1%→78.2%→96.9%, post-index verification recipe, branch discipline incl. worktree-per-branch + rebase→invalidate_cache, privacy defaults, PCTX_DATA_DIR, registration pinning + `--scope local` = git-repo-root); README + docs/README links; AGENT_REFERENCE "name the tool explicitly" deferred-tools row; docs/02 native-binary section rewritten (optional dep + tier check, "use node@22" caveat dropped). New tests: chunked-commits (4), discovery-precedence (7), hooks-defaults (4); cmdCheck tests await. Version **1.24.0**. Tasks 563–568. **Offered to the reporter:** re-run the 89k tree — durability is the unblock (their per-file rates predict ~35–90 min). Gap-analysis-v2 strawman phases shift to 92 (resolver hygiene) / 93 (visibility) / 94 (honesty sweep). See `dev-docs/PHASE91_TASKS.md`.

**Phase 90 — COMPLETE** (Offset Integrity — char-vs-byte corruption in stored symbol spans)

Trigger: gap-analysis-v2 CRITICAL 1. The old "CRLF offset drift" memory was a MISDIAGNOSIS (`\r` is 1 byte = 1 char): `parse-dispatcher` feeds tree-sitter a decoded string, so `node.startIndex` is a UTF-16 code-unit index — stored as `startByte` and then used to slice raw byte Buffers. 254/267 of PureContext's own src files (95%) had non-zero drift (em-dashes, `───` dividers, `→`); `get_symbol_source(verifyContentHash)` started ~8 lines early. Fix (canonical decision: **storage = TRUE bytes; conversion ONCE in the pipeline**): (1) **`src/core/offsets.ts`** — `buildOffsetConverter(buffer, decoded?)` (ASCII identity fast path `buffer.length === str.length` → shared zero-cost converter; surrogate pairs 2 units/4 bytes; BOM 1 unit/3 bytes; out-of-range clamps so `str.length` AND `buffer.length` both map to `buffer.length` — whole-file spans survive either convention), `convertSymbolSpans`, `lineOfByte`, `lineOfChar`, memoized `decodeCached` (WeakMap). (2) **`file-processor.ts` converts at 4 sites** (normal tree parse, C-header fallback, adapter preProcess per-BLOCK — block char indices were being added to the BYTE `offsetInOriginal`, a real mixed-math bug on SFCs with non-ASCII before the script block — and adapter non-preProcess tree branch) + `frameworkSymbols` uniformly; the android adapter's local `byteOffset` helper was deleted so ALL adapters emit char-space. **Regex-only handlers (19) already emitted true bytes (`Buffer.byteLength` line accumulation) and are NOT converted** — documented contract in the file-processor header. (3) **Audit result: every stored-span consumer was already byte-space** (Buffer slices / byte newline counts) — zero char-space consumers, so the storage fix healed all ~30 sites without touching them; 11 duplicate `byteOffsetToLine` impls consolidated onto `symbol-lines.ts` → `offsets.lineOfByte`. (4) **Bonus bug class (audit find):** go/javascript/python/ruby handlers built signatures via `Buffer.toString('utf8', charIdx, charIdx)` (byte-range API fed char indices → garbled text after multi-byte chars) — now `decodeCached(source).slice(...)` (rust/php already documented the rule). (5) **Live-re-parse tools pure char-space:** `search_ast` (+ response `startByte`/`endByte` now converted to true bytes), `get_lexical_scope_matches` (memoized decode), `search_by_decorator` — exact snippets + `lineOfChar` line numbers. (6) **Stale indexes: schema v10→v11** (values-correctness bump, no DDL); `indexFolder` force-re-parses pre-v11 repos once (empty hash cache — the Phase-89 v10 mechanism widened); `check_index_staleness` returns `schemaWarning` on pre-v11. (7) Proofs: e2e mixed-encoding fixture (BOM/CRLF/emoji/CJK/em-dash + ASCII control) — every stored symbol byte-slices losslessly + exact-declaration spot checks; **self-index of PureContext: 5,919 symbols, 0 lossy slices, `verifyContentHash` byte-exact**; `analyze_diff` wrong-symbol-attribution fixture (60-em-dash header) green. New tests: offsets (14), file-processor-offsets (6), offset-integrity-e2e (3), live-parse-charspace (3), analyze-diff attribution (1). Version **1.23.0**. Tasks 555–560. See `dev-docs/PHASE90_TASKS.md`.

**Phase 89 — COMPLETE except reporter re-validation** (Graph Integrity + Edge Hygiene — 1.18.0 verification-report follow-up)

Trigger: the reporter's UPDATED verification of 1.18.0 on the production Android automotive tree (`dev-docs/purecontext-followup-1_1.18.0.md` + chat fragment + install runbook), plus three gap-analysis-v2 amendments. Two new CRITICALS led. (1) **Task 561 — incremental re-index destroyed the graph:** `deleteEdgesByFile` deleted `source OR target` rows but only source edges regenerate → the prescribed `index_file`-after-write loop rotted incoming edges monotonically (reporter repro: edit A.kt → both B→A and C→A gone, no self-heal). Fix: new `deleteEdgesBySource` on all three REPROCESS sites; the both-directions delete only for files removed from disk. PLUS the new-file half (runbook §7): schema **v9→v10** adds `import_records` (raw specifiers persisted per file, `src/core/db/import-store.ts`); `reindexFiles` detects files that did not previously exist and re-resolves the WHOLE graph from stored records (`deleteEdgesExceptType('di')` + `buildGraph(getAllImportRecords)`) — no re-parsing; gated on repo schemaVersion ≥ 10; `indexFolder` force-reparses pre-v10 repos once (empty hash cache) to backfill. Acceptance: targeted re-index ≡ fresh full index, proven in `reindex-edge-integrity.test.ts`. (2) **Task 562 — no prune = hybrid index on branch switch:** `indexFolder` now prunes DB files absent from disk (symbols/edges/records/FTS; skipped when `limitSkipped > 0` — a truncated listing proves nothing), additive `filesPruned` on IndexResult; worktree-per-branch documented as the intended pattern (README, AGENT_REFERENCE, HARNESS-CONTRACT §2); post-checkout hook = carried stretch. (3) **Task 548 — reserved namespaces:** `graph.reservedNamespaces` config (defaults `android`/`androidx`/`java`/`javax`/`kotlin`/`kotlinx`/`dalvik`/`com.android.internal`/`sun`/`jdk`; `[]` = AOSP-fork opt-out): specifier short-circuit + registration-time exclusion of reserved-DECLARING files (go-resolver vendor-skip shape — also keeps them out of basename/symbol fallbacks); in `di-edges.ts` the check runs BEFORE `bare()` strips packages (v1 plan's "DI unaffected" was WRONG — a local `android.util.Log` shim would have provided bare `Log`). Kills the 862/541/459-edge shim targets (31% of the corpus). (4) **Task 549 — production→test edges (28.6%):** shared `src/core/test-paths.ts::isTestFilePath` replaced FIVE private copies (test-mapper canonical + change-synthesis/check-delete-safe/find-untested-symbols/symbol-risk) and adds Gradle (`src/test|androidTest|testFixtures/`) + .NET (`*.Tests/`) sets; `jvm-resolver.dropTestCandidates` runs BEFORE `preferSameModule` (a main candidate is never displaced by a same-module stub); di-edges drops test-double providers for production consumers (@TestInstallIn fakes). (5) **Task 550:** blast-radius honesty — description says "the symbol's FILE", additive `granularity:'file'` + `depth` echo + **`truncated`** flag (bfsFiles previously abandoned the queue at depth 3 silently; TraversalResult gains `truncated`, also on `get_context_bundle`). (6) **Task 551:** `PCTX_DATA_DIR` (already existed — v1 plan's new env var was dropped after the reporter's self-correction) set in `test/setup.ts` → `npm test` leaves `~/.purecontext` untouched (verified byte-identical); fixed two tests that deleted/fought the var (get-co-change restore; fetch-public-index per-test isolation — the global guard had made its manifest CACHE persist across tests, the "8 failing tests" the Phase-88 agent saw). (7) **Task 552:** NUL bytes escaped to `\u0000` in `di-edges.ts` — and the new `source-hygiene.test.ts` guard immediately found a FOURTH unreported instance (`compare-change-impact.ts:` 3 NULs in the violation dedup key, fixed); VERSION===package.json guard. (8) **Task 553:** `src/core/bootstrap-registry.ts` = THE registration list (the four hand-kept lists had drifted: index.ts 41 / worker 41 / run_benchmark 40 **with spring-boot before android, violating Phase-85 routing** / run_purecontext **21 — half the language surface**; adapter ORDER also differed main-vs-worker); CSS stays config-gated via `registerStandardHandlers({cssVariables})`; new `src/lib.ts` + package.json `"./lib"` export (`bootstrapLibrary`/`indexFolder`/`reindexFiles` — no server side effect; `.`/`bin` byte-identical). Version **1.22.0**. **Pending:** reporter re-validation on the corpus (their scripts + offered per-module hygiene numbers); benchmark reruns will shift Android/JVM edge counts (intended) and run_purecontext-based numbers (measurement-scope correction, the Phase-75 class). **Out of scope (carried):** Issue D test-mapper redesign (`skipTestMapper` documented), post-checkout hook, adoption/routing gap from the chat fragment. Tasks 548–554 + 561–562. See `dev-docs/PHASE89_TASKS.md`.

**Phase 88 — COMPLETE** (Search-Quality Repair Sweep — benchmark-driven)

Trigger: Phase-73-era stale zeros + the two known retrieval/coverage defects in BENCHMARK-RESULTS.md. Principle held throughout: diagnose retrieval vs ranking FIRST; ranking changes per-language gated only (no global formula change). (1) **Task 542 stale-zero re-runs, both old diagnoses were WRONG:** timescaledb's real killer was the `@extschema@.` PGXS placeholder (`CREATE FUNCTION @extschema@.create_hypertable` — regex failed, the entire public SQL API unindexed; fix = optional `(?:@\w+@\.)?` prefix in `sql.ts`) → **0/4/8 → 60/76/84**; rest-api-description's blamed `\w+` regex never existed (the handler is a js-yaml parser) — the real defect was `findKeyOffset` scanning the whole buffer per symbol (O(keys×bytes), 6.2 s/10 MB file, the old "3+ h, 0 files" run) → new one-pass `buildKeyOffsetIndex` (line-anchored + quoted-JSON passes, non-ASCII-safe byte offsets, old scan kept as map-miss fallback), 11×/file. (2) **Task 543 novu:** hooks were indexed but as kind `composable` (a Vue/Svelte adapter claims the React monorepo's `.ts`) and no FTS content contained the word "hook" → kind-alias FTS tokens (`hook`/`composable` kinds index "hook composable", `component` indexes "component", `symbol-store.ts`); plus the **harness had silently drifted from production** — no Phase-71 hook OR-fallback clause, no `RankOptions` to `rankSymbols` (the Phase-71 novu fixes were never measured); harness now imports the production helpers (newly exported from `search-symbols.ts`) → **0/0/0 → 28/28/40**; guard nestjs 100/100 P@3/R@5 held. (3) **Task 544 jenkins — the coverage theory was disproved:** all 22 "missed" methods were in the index; pool replay showed ranks 10–61 with `run`/`execute` at rank 1 (single generic verb hits full identityExact +60 via a query word or its verb synonym). Fixes, Java/Groovy-gated (gate auto-activates on Java-dominant pools — jenkins pools often carry zero .groovy files): generic-verb identityExact ×⅓ (`GENERIC_METHOD_NAMES`), new `camelCompoundBoost` +30 (camelCase analog of the underscore compound boost), OR-fallback on AND pools <5 (production + harness) → **4/8/12 → 36/60/72** (JC 32/48/56 — PC leads all axes); guards gradle 8/12/20→**16/32/44**, groovy 12/24/36→**32/44/44**, maven 48→**52** P@1. (4) **Task 545 Haskell (.hs/.lhs-gated):** queries announce the kind ("function that…", "record…", "sum type…") — kind hints +35 matching/−20 contradicting, spaced instance/constructor names ("ToHeaderValue PreferCount") −15 → postgrest-hs **0/28/36 → 16/40/52**, pandoc **0/4/8 → 8/20/20**. (5) **Task 546 named-root-cause spot fixes:** Swift same-file `extension` rows shared the primary type's ID and `INSERT OR REPLACE` destroyed the class row + doc comment (swift-nio's ChannelPipeline was a "Swift extension:" stub) — extension IDs now byte-position-keyed → swift-nio 0/4/4 → **4/8/12**; ObjC GNUstep `GS_GENERIC_CLASS(NSArray, …)` macro captured as class name (NSArray/NSString absent from libs-base) — normalized to first arg; scores hold 4/8/8 (conceptual-vocabulary gap, JC also ~0, timebox honoured). Guards vlc-ios/SparkleShare unchanged, vapor 20/28/32→**28/36/48**, SCA R@5 +4, excalidraw P@3 +4. (6) **Attribution discipline:** nuxt 20/28/40→4/12/24 is a **stale-baseline correction** (isolation-verified: pre-88 code + pre-88 harness scores 4/8/20; the Phase-75 `.vue`-indexing fix changed pool composition as predicted) — Phase 88 itself is net +4pp there; nestjs 84→80 P@1 is run-to-run FTS-pool-boundary variance on a 2-point margin (`EmailService.send` generic-verb identity, un-gated in TS — the global fix is deliberately deferred). Full suite green; new tests: sql @extschema (4), fts-kind-alias (5), Java/Groovy ranking (7), Haskell hints (6), swift extension-ID (1), ObjC macro (2). Version **1.21.0**. **Deliberately NOT done (carried):** flutter-scale work, Ruby ranking, global generic-verb identity fix, nuxt vocabulary pass, libs-base conceptual queries, 71 >32 MB spec files in rest-api-description. Tasks 542–547. See `dev-docs/in-progress/phase88-findings.md`.

**Phase 87 — COMPLETE** (Rust — mod-tree resolution + crate-visibility indexing)

Trigger: post-Phase-82 gap analysis — Rust had BOTH bug classes: `use crate::a::b` never resolved (zero dependency edges) and every non-`pub` item was skipped (the Kotlin-`internal` false-`no_match` chain). Kept as its own phase because Rust is the only language needing a real module TREE (mod.rs / `foo.rs` layouts, nested modules), not a flat map. (1) **Task 537 audit findings:** grouped `use a::{b,c}` emitted ONE record; `use crate::{a,b}` LOST the `crate` keyword (keyword nodes never collected); renames stored the alias, not the original; `pub(crate)` already passed the old `startsWith('pub')` gate but without metadata. **Handler extended:** `flattenUseTree` emits one record per leaf (nested groups included, PHP grouped-use pattern), `USE_PATH_TYPES` includes the `crate`/`super`/`self` keyword nodes, globs marked `importedNames:['*']`, renames carry the ORIGINAL path. (2) **`src/graph/rust-resolver.ts` (Task 538):** crate boundaries = nearest ancestor `Cargo.toml` (line-parsed `[package] name`, dash→underscore; Cargo-less repo falls back to repo root); module map derived from FILE LAYOUT under `src/` (v1: `#[path]` overrides documented limitation) — `src/a/b.rs` ≡ `src/a/b/mod.rs` ≡ `a::b`, `lib.rs`/`main.rs` = root, `src/bin/*` = root; resolution = longest module-path prefix (Go rule: first match wins, shorter prefixes never tried) → leaf via symbol table SCOPED to the module's files → module file fallback (inline `mod`/macro items; over-approximation rule); `self::`/`super::` (chains) relative to the source file's module position; bare head → own crate's top-level modules (2018 uniform paths) → workspace crate name → external; globs expand the module subtree capped by `graph.maxWildcardFanout`. (3) **Wiring (Task 539):** `rust` FamilyResolvers key + dispatch row (passes `importedNames` for the glob flag, like Python); FAMILY_DEFS row; e2e = real Cargo workspace (root crate + sibling `util-crate`) through full `indexFolder` incl. worker path — crate::/self::/super::super::/glob/workspace-name edges + blast radius + visibility metadata all asserted. (4) **Visibility (Task 540):** everything indexed — `pub` no meta, `pub(crate)`/`pub(super)`/`pub(in …)` → `frameworkMeta.visibility:'crate'`, no modifier → `'module'` (Rust has no true `private`; metadata does the filtering); cfg meta merges with visibility in one `buildMeta`. Benchmark rule fired: serde P@1 dropped 28→16.7 (>4pp) with the 5.6× bigger index, so the Phase-84 penalty was EXTENDED to `visibility:'module'` (−20 in `relevance-ranker.ts`; `'crate'` deliberately NOT penalized — crate-visible is legitimate API). With the penalty: serde 20/23.3/33.3 (PC now beats JC 10/20/30 on all three), tokio 40/40/43.3 (P@1 exactly back to the old 40 baseline), graphql-engine 52/68/76 (P@1 flat vs baseline, symbols 40,417→52,797). Remaining tokio P@3/R@5 gap vs the old 60/72 is the stale-baseline class (v1.5-era numbers, listmonk lesson) → Phase 88 sweep. (5) Docs: LANGUAGE-SUPPORT (resolver row + Rust notes + visibility rules; unresolved row shrinks to Ruby + long tail), AGENT_REFERENCE limitation row, README, docs/09, graph-coverage note (+ Rust v1.20.0 re-index hint), CHANGELOG. New tests: use-shapes (11), visibility (in rust.test.ts), rust-resolver (16, incl. temp-dir Cargo workspace), rust e2e (7); phase4 + rust.test.ts skip-expectations flipped. Version **1.20.0**. **Out of scope (carried):** macro expansion, `#[path]`, `#[cfg]`-conditional trees (all branches indexed), build.rs modules, trait-method resolution, Ruby. Tasks 537–541. See `dev-docs/PHASE87_TASKS.md`.

**Phase 86 — COMPLETE** (Declared-Module Wave 2 — PHP, Haskell, Elixir, Erlang, Fortran dependency edges)

Trigger: post-Phase-82 gap analysis — five more languages emitted unresolvable module specifiers and indexed to **zero dependency edges**. All five ride the Phase 82/84 family-resolver seam; no schema change (PHP/Haskell reuse v9 `declared_package`, Elixir/Erlang/Fortran resolve via symbols/filenames). (1) **Dispatch generalized (Task 530):** `buildGraph` now builds one **extension → resolver Map** from `FamilyResolvers` (replaces the growing if-chain; TS byte-identical + bare-`JvmResolver` back-compat both regression-guarded); `buildFamilyResolvers` is table-driven (`FAMILY_DEFS`). Sibling resolver modules chosen over parameterizing `jvm-resolver` (smallest diff; JVM/C# behavior untouched by construction). (2) **PHP** (`src/graph/php-resolver.ts` + `extractPackage` on phpHandler — first `namespace`, backslashes as written): resolution order = exact fully-qualified **symbol** match (handler stores qualified names) → declared-namespace map + basename/symbol, longest prefix → whole-namespace use capped by `graph.maxWildcardFanout` → **composer.json PSR-4 fallback** (`autoload` + `autoload-dev`, root + nested, memoized ascent like go.mod); composer.json dirs double as module roots for same-module ambiguity preference. (3) **Haskell** (`haskell-resolver.ts` + `extractPackage` — header's LAST `module`-typed child; the first is the keyword): exact declared-module → file (one module per file), dotted path-suffix fallback for headerless files. (4) **Elixir** (`elixir-resolver.ts`): module map from **symbols** (kind class/interface, qualified `defmodule` names — multi-module files legal); exact match then longest-known-prefix fallback. (5) **Erlang** (`erlang-resolver.ts`): module == basename (`mod:fun` → `mod.erl`, collisions → all candidates); handler now also emits `-include`/`-include_lib` (path literal) → resolved by `.hrl` basename; the `:` check runs BEFORE the path check (arity slash in `fun/0` would misroute). "Always external" hardcoding removed. (6) **Fortran** (`fortran-resolver.ts`): `USE name` → files whose symbol table declares `MODULE name` (kind class + `MODULE ` signature discriminator; case-insensitive). (7) Honesty note in `graph-coverage.ts` updated (unresolved = Rust/Ruby/long tail + per-family re-index versions). Docs: LANGUAGE-SUPPORT (5 rows moved up + Wave 2 notes), AGENT_REFERENCE limitation row, README, docs/09, CHANGELOG. New tests (47): extract-package PHP (4) + Haskell (3), php-resolver (8), haskell-resolver (6), elixir-resolver (7), erlang-resolver (7), fortran-resolver (5), wave2 e2e (6 — one mixed 5-language fixture repo through full `indexFolder`, edge + blast radius per language, incl. the Erlang include edge). Suite **6,312 green**, 0 regressions. Version **1.19.0**. Benchmark repos re-indexed for graph availability (edge counts recorded in PHASE86 notes; search scores deliberately not re-scored — edges are the deliverable). **Out of scope (carried):** Rust (Phase 87), Ruby, Gleam/Lua/R/GDScript edges, PHP conditional `require` includes. Tasks 530–536. See `dev-docs/PHASE86_TASKS.md`.

**Phase 85 — COMPLETE** (Android Framework Adapter — Compose, Hilt/Dagger DI, Manifest, Gradle modules)

Trigger: the Kotlin evaluation report's "nice to have" (`dev-docs/purecontext-kotlin-support-request.md` §Android adapter), unblocked by Phase 82. New `android` adapter (`src/adapters/android.ts`), registered in all three registries **ahead of the other JVM adapters** (processFile routes to the FIRST matching adapter — android must win `.kt`/`.java` on Android repos; a Ktor/Spring repo is unaffected since detect() keys on Android markers: `AndroidManifest.xml` anywhere OR `com.android.application`/`.library` in a `build.gradle(.kts)`, bounded recursive scan sibling of `scanForFramework`). **Stateless by construction** (Phase-75 worker lesson): everything derives from file path + content; the only module state is a bounded per-file annotation-facts cache written in `extractFrameworkSymbols` and read synchronously by `enrichMetadata` in the same processFile call (no await between them). (1) **Compose** — `@Composable` funs upgrade to kind `composable` via enrichMetadata (React-adapter pattern: id recomputed, handler spans/signatures preserved — NOT spring-kotlin's merge-by-id which clobbers spans); `@Preview` → `frameworkMeta.preview: true`; type-position guard so `content: @Composable () -> Unit` doesn't mark the next fun. (2) **Hilt/Dagger DI meta** — event-scan (annotations + declarations sorted by position, pending-annotation state machine) over Kotlin AND Java: `@Module`/`@Provides`/`@Binds`(+`providedType`, binds' param = consumed impl)/`@Inject` ctor+field (+`consumedTypes`, generics/qualifiers stripped)/`@HiltViewModel`/`@AndroidEntryPoint`/scope annos → `frameworkMeta.di`. (3) **DI edges** — `src/graph/di-edges.ts` `buildDiEdges(db, repoId)`, pure thin consumer called at BOTH index-manager graph-build sites gated on the android adapter being active: bare-name match of provided vs consumed types; `@Inject`-ctor classes are providers of their own type; ambiguous → edges to ALL providers (Phase-82 over-approximation); same-file pairs skipped; repo-wide delete-then-rebuild (`deleteEdgesByType(db, repoId, 'di')`) keeps targeted re-index ≡ full index; edge shape `edge_type:'di'`, `specifier:'di:<TypeName>'`. **`find_cycles` excludes `di` edges** — fixture evidence: `@Binds` makes module ↔ impl edge pairs BY DESIGN, every binding would be a false 2-cycle. (4) **Manifest entry points** — `<activity>/<service>/<receiver>/<provider>` → `route` symbols (byte-offset-correct spans; leading-dot names resolve against the manifest `package` attr, gradle-namespace manifests fall back to the bare name — documented v1 limitation); `get_entry_points` gains kind `android_component` (`'route'` added to candidate kinds — additive, other rules don't match routes), LAUNCHER activity high-confidence first. (5) **Gradle modules** — `frameworkMeta.gradleModule` (`:app`, `:feature:login`, `:` root) from path segments before the first `src/` (worker-safe, no settings.gradle read); layer-config recipe documented in FRAMEWORK-ADAPTERS.md. (6) **Pipeline fix (root-cause class):** `processWithAdapter` always tree-sitter-parsed — an adapter-routed file owned by a regex-only handler (manifest → xmlHandler, `grammarPath() === null`) THREW and lost every symbol; adapter path now mirrors the normal path (null-grammar branch + handler `detect()` gate). Docs: FRAMEWORK-ADAPTERS.md Android section + layer recipe, docs/08, LANGUAGE-SUPPORT resolver row, README. New tests: android unit (35: detect/fileFilter/kotlin+java facts/manifest/enrich/gradleModule/cleanType), di-edges (9), android e2e (7: compose upgrade through the WORKER path, 4 manifest components + launcher, 4 cross-language di edges, find_cycles exclusion, entry-points ranking, reindexFiles di parity). Version **1.18.0**. **Out of scope (carried):** `res/**` XML, Gradle version catalogs, KSP output, NavHost route tracing, `@Named` qualifier disambiguation, interface-hierarchy DI matching. Reporter validation on the 26k-file automotive codebase pending. Tasks 523–529. See `dev-docs/PHASE85_TASKS.md`.

**Phase 84 — COMPLETE** (Python + Go Dependency Edges + Go unexported symbols)

Trigger: post-Phase-82 gap analysis — Python and Go were the two highest-volume ecosystems still producing **zero dependency edges**; Go additionally had the Kotlin-`internal` bug class (unexported names skipped entirely → false `no_match`). Fix, no schema change (Python/Go module identity derives from paths + `go.mod`, not a stored header): (1) **Python resolver** (`src/graph/python-resolver.ts`) — dotted-name → files index built once per graph build (`a/b.py` ↔ `a.b`, `__init__.py` = the dir); root-stripping for `src/` and any first-level dir WITHOUT `__init__.py` (a first-level dir WITH one is part of the module path — never stripped); relative imports (`.`/`..pkg`) resolve by exact directory walk; `from a.b import c` prefers submodule `a/b/c.py`, else the module file with a symbol-table tiebreak; unknown module → external → no edge. Handler audit (Task 516) found relative-import emission already correct — 7 exact-shape tests added. v1 limitations documented: no `sys.path`, editable installs, `pyproject` package-dir remapping. (2) **Go resolver** (`src/graph/go-resolver.ts`) — every `go.mod` above an indexed `.go` file (memoized readFileSync + `module` regex) maps module path → dir; longest prefix wins (workspaces + `/v2` suffixes work); an import resolves to **every** indexed `.go` file of the target package dir (the true package semantic — no fanout cap, no symbol-table fallback needed); `_test.go` included; no edges into `vendor/`. (3) **Wiring** — `buildGraph` 4th param widened to `FamilyResolvers` `{jvm?, python?, go?}` (bare `JvmResolver` still accepted — throwing-stub TS parity guard passes); Python's resolve() additionally takes `importedNames`; both `index-manager` sites share new `src/graph/family-resolvers.ts::buildFamilyResolvers` (each family built only when the batch has its files, after persistence). (4) **Go unexported indexed** (all six extraction sites: functions/methods/types/consts/vars/interface methods) with `frameworkMeta.visibility: 'unexported'`; exported names carry nothing; `get_public_api` unaffected (keys on the TS `export` keyword). Mild ranker penalty −20 for `visibility:'unexported'` (`relevance-ranker.ts`, mirrors library-path penalties; identityExact +40 still wins for exact-name lookups). (5) Docs: LANGUAGE-SUPPORT (two new resolver rows + Go visibility), AGENT_REFERENCE limitation row, README, docs/09. New tests: python import shapes (7), python-resolver (16), go-resolver (9), python e2e (5, incl. blast-radius smoking gun), go e2e (6, incl. unexported visibility), ranker penalty (4); go.test.ts + phase3 integration expectations updated. Suite **6,214 green**, 0 regressions. Benchmarks (baselines were stale v1.2–v1.4 runs): jcodemunch P@1 40→40, R@5 56→60; vismedic P@1 28→24, P@3 40→44, symbols 2,297→4,186; saleor P@1 72→64, P@3/R@5 flat 88/96, symbols 27,719→28,112; listmonk 28/60/72 → **4/16/40 — but an isolation run (pre-84 Go handler on current code) scores IDENTICALLY, so Phase 84 contributed 0pp**: the regression accrued unnoticed between v1.2→v1.16 (stale-benchmark class → Phase 88 search-quality sweep). Go symbols 1,357→1,651. Tasks 516–522. Version **1.17.0**. **Out of scope (carried):** PHP/Rust/Haskell/Elixir/Erlang/Fortran resolution, `sys.path`/editable installs/`pyproject` remapping, Go build tags/cgo, Ruby. See `dev-docs/PHASE84_TASKS.md`.

**Phase 83 — COMPLETE** (C# Parity — visibility fix + namespace resolution)

Trigger: post-Phase-82 gap analysis (`dev-docs/in-progress/language-framework-gap-analysis.md`) — C# had BOTH Phase-82 bug classes, and the visibility one was worse than Kotlin's: only explicit `public`/`protected` symbols were indexed, so `internal` AND modifier-less types (implicitly internal — very common) were missing entirely (explains the 8–12% C# benchmark P@1). Fix, no schema change (reuses v9 `declared_package`): (1) **Visibility** (`src/handlers/csharp.ts`) — `isPublicOrProtected` replaced by `visibilityOf` returning the explicit modifier combo or null; **asymmetric C# defaults respected**: top-level/nested type with no modifier ⇒ internal ⇒ INDEXED (`frameworkMeta.visibility`), member with no modifier ⇒ private ⇒ skipped; explicit `private`/`private protected` skipped at both levels; interface members keep the implicit-public bypass. (2) **`extractPackage`** on `csharpHandler` — file-scoped or OUTERMOST block `namespace` → `declared_package` (nested blocks store the outermost name only; inner names resolve partially via the symbol-table fallback — documented v1 limitation); zero pipeline work, Phase-82 plumbing picks it up. (3) **CLR resolution on the existing resolver** — `.cs` added to the extension set (renamed `DECLARED_MODULE_EXTENSIONS`/`isDeclaredModuleSourceFile`; deprecated `JVM_EXTENSIONS`/`isJvmSourceFile` aliases kept); module markers now via memoized `readdirSync` per dir (exact Gradle/Maven names OR `*.csproj`/`*.sln` suffix); **new config `graph.maxWildcardFanout` (default 100, 0=uncapped)** caps package-wide expansions (every C# `using X.Y` is a namespace wildcard) — first N in deterministic (sorted) order + one warn per build; applies to JVM wildcards too (shared path). (4) Wiring verified through both `index-manager` sites + e2e (`test/graph/csharp-graph-e2e.test.ts`: two `*.csproj` projects, file-scoped + block namespaces, cross-project `using`, `using static`, external `using System` dropped, blast radius returns importers, internal class findable with visibility metadata, worker-path `declared_package` parity). (5) Docs: LANGUAGE-SUPPORT (declared-module resolver row + C# visibility rules), AGENT_REFERENCE limitation row, README pointer, docs/09. New tests: csharp-visibility (12), extract-package C# (4), jvm-resolver C# + fanout cap (5), csharp e2e (6); csharp.test.ts internal-class expectation updated. Benchmarks (coverage-driven, as planned): fleetdirect-saas **5,163→12,837 symbols** (2.5×), P@3 16→24%, R@5 20→36% (P@1 8→4%: new same-name symbols compete at rank 1 — ranking follow-up parked for Phase 88); origamicms **15,385→24,726 symbols**, P@1 12→28%, P@3 16→40%, R@5 16→52%. Suite **6,167 green**, 0 regressions. Tasks 509–514. Version **1.16.0**. **Out of scope (carried):** VB.NET/F#, ASP.NET adapter, Razor, `global using` propagation. See `dev-docs/PHASE83_TASKS.md`.

**Phase 82 — COMPLETE** (JVM Graph Resolution — Kotlin/Java/Scala/Groovy dependency edges)

Trigger: external evaluation on a 26k-file Kotlin/Android codebase (`dev-docs/purecontext-kotlin-support-request.md`, verified claim-by-claim against source). Root cause: bare JVM specifiers (`com.example.Bar`) hit path-resolver strategy 3 → `null` → **every JVM repo indexed to 0 dependency edges**, silently disabling ~17 graph tools (`get_blast_radius`, `find_importers`, `find_cycles`, arch tools, the centrality axis of `get_symbol_risk`, …) while the suite stayed green (no test asserted a JVM edge; benchmarks measure search precision, not graph recall). Fix, all read-side: (1) **`extractPackage`** — new optional `LanguageHandler` method, implemented in Kotlin (`package_header`), Java (`package_declaration`), Scala (chained clauses concatenate), Groovy (regex, stops at first code line); threaded through `processFile` → worker → `upsertFile` into new **`files.declared_package`** (schema **v8→v9**, additive; the index lives ONLY in the migration — a base-DDL index would crash existing v8 DBs; `upsertFile` tri-state: `undefined`=keep, `null`=clear). (2) **`src/graph/jvm-resolver.ts`** — package→files + package→symbol-name maps built once per graph build; longest-package-prefix resolution → basename match → symbol-table fallback (Kotlin member imports); wildcard handling per handler shape (Kotlin/Java wildcards arrive as **bare package names**, Groovy `.*`, Scala `._` + `{A, B => C}` selectors); cross-module ambiguity prefers the importer's own Gradle/**Maven** module (`build.gradle`/`build.gradle.kts`/`pom.xml`, memoized disk walk), else edges to **all** candidates (over-approximation is the safe direction for blast radius); self-edges filtered; NULL-package rows (pre-v9) fall back to `src/<set>/<lang>` path convention, no-root ⇒ no guess. (3) **`buildGraph(…, jvmResolver?)`** — extension dispatch; both `index-manager` sites build it only when the batch has JVM files (zero cost on TS repos); TS output **byte-identical** (regression-guarded with a throwing-stub resolver). (4) **Kotlin `internal` indexed** (skip only `private`; `frameworkMeta.visibility` records internal/protected) — kills the false `no_match` → confident-wrong-answer chain. (5) **Honesty signals:** `index_folder` returns `limitReached`+`totalBeforeLimit` (silent 10k truncation fix); graph tools (blast-radius/importers/bundle/cycles/coupling) attach `graphCoverage:'empty'` + note when a ≥20-file repo has 0 edges; `.gradle`/`.idea` added to `BUILT_IN_EXCLUDES`. (6) **Docs:** LANGUAGE-SUPPORT.md per-language edge matrix (replaces the false "every language" claim), AGENT_REFERENCE limitation row, README pointer, docs/09 external-vs-unresolvable distinction. New tests: extract-package (17), jvm-resolver (17), jvm-graph e2e (9, real 2-module Gradle project through full `indexFolder` incl. worker path — blast radius returns all importers), kotlin-internal (7), limit/excludes (3), graph-coverage (3). Suite **6,140 green**, 0 regressions. Tasks 499–508. Version **1.15.0**. **Boundaries:** Python/Go/PHP/Rust resolution deferred (the seam makes them additions); Android adapter (Compose/Hilt/manifest) deferred; reporter validates the branch on the 26k-file codebase. See `dev-docs/PHASE82_TASKS.md`.

**Phase 81 — COMPLETE** (Active Context Reconstruction — `get_task_context` associative upgrade)

Trigger: the NUS **MRAgent** / **MemR³** agentic-memory papers (VentureBeat 2026-06-26 — 118K tok/query vs LangMem 3.26M). Their transferable idea is the *retrieval architecture* (active multi-step walk over a tiered Cue→Tag→Content graph + an evidence-gap signal), NOT their conversational-memory domain (deliberately out of scope — crowded commodity). Tiered cheap-tier-before-content read was already PureContext's founding thesis; **this phase closes the gap: active associative walk + evidence gaps.** Smoking gun: `get-task-context.ts` was textbook **single-shot retrieve-then-reason** — top-30 lexical/semantic discovery → one AI selection — importing *none* of the graph engines, so a structurally-essential-but-lexically-dissimilar symbol (a dependency, a caller, a co-change partner) could never be selected. Upgrade (all in `get-task-context.ts` + config; thin consumer, no new engine): (1) new `mode?: 'flat' | 'associative'` (default **associative**; `mode:'flat'` is byte-identical to pre-81, regression-guarded). (2) **Stage 1.5 `expandSeeds`** (pure, exported) — for the top `seedCount` discovery hits, walks `getContextBundle` (→`imports`), `getBlastRadius` (→`importedBy`; **not** `find_references`, which is a full-repo content scan), and `getCoChange` (→`coChange`, ≤`maxSymbolsPerPartner` symbols per ≤`maxCoChangePartners` partner files, gated on `countCommits>0 && signalQuality!=='low'`, memoized per filePath) into a deduped pool with **provenance** (first-writer-wins), capped to `maxPool` (discovery first, then expansion by provenance weight `importedBy>imports>coChange`). (3) **Roles derived from the graph edge** (`imports`→dependency, `importedBy`→caller, `coChange`→historical, seed→primary) — AI keeps only the prose `relevanceReason`; ContextRole widened with `caller`/`historical`. (4) **Provenance fed to the AI prompt** (`buildPlanningPrompt` gains an optional provenance arg; undefined ⇒ identical flat output) — and the **no-AI/no-embeddings fallback now ranks by provenance** (pool order), so the floor rises from "top-N keyword guess" to "seeds + real graph neighborhood", pure SQL. (5) **New additive output:** `provenance` per item, top-level `evidenceGaps` (`lowConfidenceSeeds`/`droppedByBudget`/`unselectedCoChange` — the last suppressed on low signal) + `suggestedProbes[]`, and `_meta.{mode,seedsExpanded,poolSize}`. **Agent is the router** (one associative round, return the gap; the harness LLM decides whether to loop) — no server-side reflective loop in v1 (protects no-AI-key / low-token value; judgment-not-actuation). Config **`taskContext.{seedCount=8, expansionDepth=1, maxPool=60, maxCoChangePartners=5, maxSymbolsPerPartner=5}`**. Serves Phase 80's cold-start orientation. 14 new tests (expandSeeds 4, associative output 3, flat parity 1, prompt provenance 2, config 2, + role-consistency); full suite **6082 green**, 0 regressions. Tasks 494–498. Version **1.13.0**. **Boundaries (carried):** no server-side reflective loop, no conversational/episodic memory (structural only; if ever added, key to symbol IDs), no new analysis engine, `find_references` not used for expansion. See `dev-docs/PHASE81_TASKS.md`.

**Phase 80 — COMPLETE** (Harness Loop Fit — make PureContext usable inside automated codegen harnesses)

Product line: *PureContext is the persistent cross-process brain a harness lacks — it tells the generating agent what already exists, what it's drifting from, and what it forgot, without re-reading the codebase from scratch every run.* Two tracks: **greenfield** (project built issue-by-issue from scratch — no git history early, so value = consistency over the growing codebase) and **brownfield** (reuse the Phase 76–79 closed loop; packaging only). Reference harness studied: `d:\private\govproxy\govproxy` (`run-issues-panel.ps1` → fresh `claude -p` per issue, 8 phases, multi-lens review). **Driving spike finding:** `index_folder` is **discovery-bound** (stats all files: ~11s no-op on 582 files) → NOT viable per-edit mid-run; a cheap single-file re-index is the #1 prerequisite. **Group A — index freshness (linchpin):** (1) **`index_file`** (`src/server/tools/index-file.ts`) — targeted single-file re-index, thin consumer of the existing core `reindexFiles`; relativizes paths, splits changed-vs-deleted by existence, discovers+caches adapters per repo; also fixed `reindexFiles` empty-file parity gap (clears stale rows when an edit removes a file's last symbol). (2) PostToolUse hook now calls a new **`index-file` CLI subcommand** (`cmdIndexFile`) instead of full `index-folder` (bootstraps a full index when the repo is unindexed); updated `src/cli/hooks.ts` + `scripts/hooks/purecontext-index-hook.mjs`. (3) **27-file no-op churn fixed** — `indexFolder` both paths used to `continue` past `upsertFile`/`cache.set` for 0-symbol/0-import files → never hash-persisted → re-parsed every run; now always persists → true no-op = 0 files. (4) **`check_index_staleness`** — per-file fresh/stale via stored-hash compare (no discovery), or repo-level summary. **Group B — greenfield consistency brain:** (5) **`check_consistency`** (`src/server/tools/check-consistency.ts`) — pre-write front door (mirror of `prepare_change`, zero history): `duplicates`/`patternFit`/`placement`/`existingApiPointer`, `signalQuality:'low'` suppresses dedup on a sparse index. **Structural-FIRST** (`mode:'structural'`, name/signature overlap — no embedding provider required; this resolves the embedding-provider open question and folds in the Task-485 degrade). Orientation (Task 484) **satisfied by the existing `get_task_context`** (FTS5 fallback = the no-embedding cold-start surface) — composed, not duplicated. **Group C — brownfield packaging:** (6) **normalized gate envelope** (`src/server/tools/gate-envelope.ts`) `{ gate: pass|warn|block, gateReasons[], nextAction }` derived additively on `prepare_change`/`verify_change`/`compare_change_impact`/`check_consistency`; (7) composite **`merge_readiness`** (`src/server/tools/merge-readiness.ts`) folds verify (completeness) + compare (arch regression) into one `{ gate, reasons[], unresolved[] }` — thin consumer, no new analysis; (8) **`docs/HARNESS-CONTRACT.md`** — greenfield + brownfield loop recipes, freshness rules, stable gate contract. Config `consistency.{maxDuplicates=5,maxPatternFit=5,maxApiPointer=20}`. **Group D (govproxy wiring) deliberately NOT done — gated on explicit go-ahead** (edits a separate repo). New tests: index-file(7), index-file-cli(2), churn(1), staleness(6), check-consistency(6), gate-envelope(21), merge-readiness(3). Version **1.12.0**. **Boundaries (carried):** no edit/build/test/merge actuation; semantic invariants stay with review lenses; PC owns structural+retrieval only. See `dev-docs/PHASE80_TASKS.md`.

**Phase 79 — COMPLETE** (Refactoring Superiority — closed pre/post loop + architecture regression)

The competitive wedge after the token-savings/jCodeMunch review (2026-06-14): stop competing on token savings (a parity feature jCodeMunch games harder) and stop chasing its larger refactoring *tool count* (`apply_rename`/`preview_refactoring`/runtime traces). Win on **judgment, not actuation** — the agent already has a file-write tool, so PureContext is the brain that says what's safe and what you forgot, not a second editor. Three new tools, all thin consumers of the Phase 77A `synthesizeChange` engine: (1) **`prepare_change`** (`src/server/tools/prepare-change.ts`) — pre-edit verdict for a stated intent (target resolution via symbolId or query + predicted change set → risk/`missingCoChange`/tests/coverageGaps/arch flags + `reasons[]` + `predictionId`); `ambiguous_target` verdict + `candidates[]` when a query has no clear winner, `no_target` when nothing matches — never synthesizes on a guess. Predicted files: rename/delete add `find_references` sites, extract adds importers, modify = target file. (2) **`verify_change`** (`src/server/tools/verify-change.ts` — the feature jCodeMunch lacks) — reuses `analyze_diff` verbatim for the actual-side synthesis, then reconciles against the prediction (`unaddressedCoChange`/`addressedCoChange`/`unplannedChanges`/`coverageGapsRemaining`; verdict `complete`/`incomplete`/`scope_expanded`); **stateless** — the agent passes `predictedFilePaths`+`predictedCoChange` back inline; co-change reconciliation suppressed when `signalQuality:'low'`. (3) **`compare_change_impact`** (`src/server/tools/compare-change-impact.ts`) — before/after architecture *regression delta* (`newCycles`/`newLayerViolations` + `resolvedCycles`/`resolvedLayerViolations`; verdict `regressed`/`improved`/`unchanged`/`no_baseline`), distinct from 77A's current-state flags. Baseline = `get_architecture_snapshot` (extended to store cycle membership + layer violations in its metrics JSON — additive, old snapshots → `no_baseline` graceful degrade). Cross-cutting: explainable `reasons[]` on every verdict (vs jCodeMunch's bare confidence number); config `refactoring.maxCandidates` (default 5); `WHY-PURECONTEXT.md` closed-loop + judgment-not-actuation positioning. 14 new tests (prepare 7, verify 4, compare 3); full suite 6012 green, 0 regressions. Tasks 467–478. Version **1.11.0**. **Boundaries (deliberately NOT built):** edit actuation, runtime traces, author/ownership metrics. See `dev-docs/PHASE79_TASKS.md`.

**Phase 78 — COMPLETE** (Node-version independence — WASM SQLite fallback + startup guard + global-Node install pinning). Tier chain `better-sqlite3` → `@sqlite.org/sqlite-wasm` (both have FTS5; `node:sqlite` does not); `node-guard.ts`/`bin.ts` fail clearly below Node 18; install pins the server to the user's global Node (Volta-aware). Version **1.10.0**. See `dev-docs/PHASE78_TASKS.md`.

**Latest: Phase 77 (Phase A) — COMPLETE** (Change-Impact Synthesis — `analyze_diff` upgrade + shared synthesis core)

Tasks 460–466 complete. Turns `analyze_diff` from "changed symbols + blast radius + a 4-line priority heuristic" into an **impact-aware change report**, built on a reusable **change-synthesis core** that Phase B's `prepare_change` will consume verbatim. Product line: *PureContext reviews changes by impact, not by diff size.* All server/tools + config + docs — no handler/adapter/core-schema changes, no new git capture (reuses Phase 76's `commit_files`). (1) **`RiskContext` perf refactor** (`src/server/tools/symbol-risk.ts`): `buildRiskContext(db,repoId)` computes the repo-wide distributions (90d churn map, afferent-coupling lookup, complexity distribution, test-file content set, co-change window) **once**; `computeSymbolRiskWithContext` scores N symbols against it; `computeSymbolRisk` is now a thin wrapper (output **byte-identical** — regression-guarded). Eliminates the O(symbols × repo) blowup on big diffs. (2) **`change-synthesis.ts`** — pure, MCP-free `synthesizeChange(db,repoId,input)` returning `aggregateRisk` (max band + top offenders), **`missingCoChange`** (the headline new signal: files historically coupled to the edited ones but ABSENT from the diff — the "you forgot to touch X" instinct; suppressed entirely on `signalQuality:'low'`), `recommendedTests`, `coverageGaps`, and `architecturalFlags` (cycles / layer crossings the change *currently* sits on — **flags, not regressions introduced**). One shared `RiskContext`; every section capped; ranks symbols-to-score by afferent coupling when over `maxSymbolsScored`. (3) **`analyze_diff` enriched** — additive `risk`/`missingCoChange`/`recommendedTests`/`coverageGaps`/`architecturalFlags`/`signalQuality` fields; `reviewPriority` now folds in risk band + coverage gaps (same `low|medium|high|critical` enum); four flags (`includeRisk`/`includeCoChangeGaps`/`includeTests`/`includeArchitectureFlags`) default **on**, switchable off (all off ⇒ today's exact shape). (4) **Config** `changeSynthesis.{coChangeConfidenceThreshold=0.4, maxSymbolsScored=25, maxCoChangeGaps=10, maxRecommendedTests=15}`. **Explicitly deferred:** `prepare_change` (Phase B), true before/after architecture regression (`compare_change_impact`), intent parsing, author/ownership metrics (permanent boundary). 34 new tests (symbol-risk context path, change-synthesis, analyze-diff fixture, config). Version **1.9.0**. See `dev-docs/PHASE77_TASKS.md`.

**Phase 76 — COMPLETE** (Temporal Risk Intelligence — co-change + composite symbol risk)

Tasks 451–459 complete. Adds the one capability the static graph cannot derive — **temporal coupling** (files that historically change together) — and fuses PureContext's existing risk primitives into a single explainable **symbol risk** verdict surfaced before edits. (1) **Co-change capture** (`src/core/git-log-reader.ts` `readRepoCommitFiles` + `src/core/db/co-change-store.ts` `commit_files` table): a SINGLE repo-level `git log --no-merges --name-only -n N` at index time, stored separately from `git_metadata` (whose per-file last-10 window is too shallow/recency-skewed for co-change). Gated on `git.coChangeDepth > 0` (default 300; `0` = byte-identical pre-phase behavior), additive + failure-tolerant. Schema **v7→v8** (additive `commit_files` table; old indexes load without re-index). (2) **`get_co_change`** tool + `src/server/tools/co-change.ts` query module: explainable `support`/`confidence`/`lift`; mega-commit filter (`git.megaCommitThreshold` default 30) + `1/(k−1)` down-weighting; `minSupport` floor; `signalQuality:"low"` on sparse history. File-granular (symbolId → file). (3) **`get_symbol_risk`** tool + `src/server/tools/symbol-risk.ts`: blends churn / centrality (afferent coupling + reverse blast radius) / complexity / test-gap / co-change spread, each **repo-relative midrank-percentile** normalized; config-weighted (`risk.weights`) sum → 0–100, banded low/review/high; always returns `factors` (raw+normalized) + `reasons[]`. Shared `src/server/tools/symbol-lines.ts` (byteOffsetToLine) extracted from get-churn-metrics. (4) **Guardrails:** opt-in `includeRisk` flag on `search_symbols`/`get_symbol_source` (compact `{band,riskScore}`, default off); `get_context_bundle` now returns `historicalNeighbors` (co-changing files not reachable via imports) when co-change data exists, else byte-identical. **No author/ownership/productivity metrics — deliberate product boundary.** 30+ new tests (co-change-store, co-change query, symbol-risk, tool e2e, parse). Version **1.8.0**. See `dev-docs/PHASE76_TASKS.md`.

**Phase 75 — COMPLETE** (Adapter-extension wiring fixes + Svelte/Astro support). Tasks 440–450 complete. **Root cause from the `.vue not indexed` investigation:** `indexFolder()` never called `discoverAdapters()` (fixed in 1.6.0 — adapters now auto-discovered when `options.adapters` is absent). Phase 75 closes the remaining instances of the same bug class and adds two new SFC adapters. (1) File watcher (`file-watcher.ts`) and GitHub remote-index path (`index-repo.ts`) now union `getAdapterExtensions(getRegisteredAdapters())` into their handler-extension sets, so `.vue`/`.svelte`/`.astro` edits trigger re-index and remote SFC blobs aren't dropped. New exported `watchedExtensions()` helper for testability. (2) Benchmark harness (`run_benchmark.ts` + `run_purecontext.ts`) now imports all framework adapters for self-registration — previously it registered 40 handlers but 0 adapters, so it had **never** indexed `.vue` (historical Vue/Nuxt benchmark numbers measured only `.ts`/`.scss`). Expect Vue/Nuxt benchmark deltas on next run — measurement-scope correction, not a regression. (3) New **Svelte** adapter (`.svelte`): `splitSvelteSFC` extracts `<script>`/`<script context="module">` blocks; component symbol per file; `useXxx`→composable. (4) New **Astro** adapter (`.astro`): `splitAstroSFC` extracts leading `---` frontmatter as a TS block; component symbol per file. Both use a shared `detect-utils.ts` (bounded recursive monorepo detection mirroring Vue/Nuxt; Vue/Nuxt keep their own inline copies). Registered in all three registries (`src/index.ts`, `indexing-worker.ts`, both harness files). 39 new tests (svelte/astro preprocessor + adapter + watcher). Version bumped to **1.7.0** (from actual 1.6.0). See `dev-docs/PHASE75_TASKS.md`.

Phases 51–75 summaries and all earlier history live in `dev-docs/PHASE*_TASKS.md` (one file per phase) and the auto-memory index. Current real version: **1.24.0**.

---

## Decision Log

Recent significant decisions:

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-08-26 | Phase 91: batched commits replace the single outer transaction; WAL checkpoint per batch | The spinning-disk single-transaction optimization became the scale wall: nothing committed until the END, so a killed 89k-file run (110 min) lost everything, and FTS5-vs-growing-WAL was the super-linear term. Per-batch transactions (default 500 files) keep small repos at effectively one commit while giving big runs durability + a monotonically growing main .db; `wal_checkpoint(PASSIVE)` per batch stops WAL ballooning without blocking readers; the parallel path also gains a memory cap (one batch of parse results in flight). Resume needed zero new machinery — the content-hash skip cache already did it, it just never got committed data to work with. |
| 2026-08-26 | Phase 91: exclusion precedence = built-ins → .gitignore → user patterns (user LAST); report whole-directory drops | The `ignore` package gives later rules negation precedence, and .gitignore-added-last meant NO user config could rescue a directory a repo .gitignore hid (verified: a nested repo silently dropped). User intent must outrank repo defaults, and built-ins stay first so users can still `!node_modules/` them deliberately. The honesty signal reports only gitignore/config drops at top level (builtin drops are expected noise) — complements gap-analysis H7 without building the full per-file drop report. |
| 2026-08-26 | Phase 91: installer safe-by-default — hooks opt-in, reminder double-opt-in, absolutist rules softened; all 7 hook events kept (verified documented) | The runbook's complaint was surprise and noise, not capability: default `install` now registers + writes instructions only (`--with-hooks` opts in); the per-edit stderr reminder trains users to ignore output so it's a separate `--with-reminders`, and a default re-run REMOVES it (converges, never leaves surprise state); the four "undocumented" events (WorktreeCreate/WorktreeRemove/TaskCompleted/SubagentStart) were verified against current Claude Code docs and ALL exist — deletion would have been wrong. agent-rules.md rewritten defaults-with-exceptions because the chat fragment showed absolutist rules get rationalized away wholesale rather than followed partially. |
| 2026-08-26 | Phase 91: better-sqlite3 → optionalDependencies; config --check reports the active SQLite tier | A failed native build aborted the ENTIRE npm install on Node versions without a prebuild — the Phase-78 WASM runtime tier existed but was unreachable from a normal install. As an optional dep the build failure is non-fatal and the tier chain takes over; the risk (silently slower WASM) is countered by making the active tier visible: config --check now async-inits the real backend and prints it, and the postinstall canary says "NOT fatal — WASM fallback" instead of prescribing a Node downgrade. |
| 2026-08-26 | Phase 90: canonical storage = TRUE byte offsets, converted ONCE in file-processor; handlers keep emitting node indices | Tree-sitter node indices are UTF-16 code units (decoded-string parsing), and ~200 handler sites store them — converting in each handler is unmaintainable, while converting at the shared pipeline choke point fixes every emitter at 4 sites. Regex-only handlers already computed true bytes (Buffer.byteLength line accumulation) and are exempt; all adapters were normalized to char-space (android's local byteOffset helper deleted) so frameworkSymbols convert uniformly. ASCII files take a shared identity converter (byte-identical output by construction, regression-guarded). |
| 2026-08-26 | Phase 90: pre-v11 indexes heal by schema-version force-re-parse, not by migration or docs caveat | Corrupted span VALUES cannot be fixed without re-parsing (no migration can recover char→byte without the source), and a docs-only caveat leaves agents silently reading shifted source. The Phase-89 v10 mechanism (empty hash cache in indexFolder when repo schemaVersion < N) was widened to < 11 — one full re-parse on next index_folder, plus a `schemaWarning` from check_index_staleness until then. |
| 2026-08-26 | Phase 90: query-time re-parse tools operate purely in CHAR space; never mix node indices with Buffers | search_ast/search_by_decorator/get_lexical_scope_matches parse fresh at query time — converting to bytes there is pointless work; the bug was slicing Buffers (or counting buffer newlines) with char indices. Rule: decoded string slices + lineOfChar for lines. The one byte-space output (search_ast's startByte/endByte response fields) IS converted, because the field names promise bytes. Same rule applied to the handler-internal signature builders (go/js/py/ruby) the audit caught slicing buffers with `toString('utf8', charIdx, charIdx)`. |
| 2026-08-26 | Phase 89: reprocess deletes SOURCE edges only; both-directions delete is the DELETION path's alone | The reprocessed file's new parse regenerates exactly its outgoing edges — its incoming edges belong to other files' parses and cannot regenerate, so deleting them (the old `deleteEdgesByFile` on every re-index) rotted the graph monotonically under the prescribed `index_file`-after-write loop, invisibly (`graph_coverage` keys on repo-wide edge count). Two functions with one owner each (`deleteEdgesBySource` / `deleteEdgesByFile`) make the contract impossible to mix up again. |
| 2026-08-26 | Phase 89: persist raw import records (schema v10) and re-resolve the whole graph when a NEW file arrives, rather than per-file patching | A new file can satisfy imports that UNCHANGED files wrote before it existed — no per-file edge repair can find those importers without their specifiers. Stored records make the full re-resolve pure map-lookup work (no re-parsing), mirror the DI delete-then-rebuild pattern, and give targeted-re-index ≡ full-index BY CONSTRUCTION. Gated on repo schemaVersion ≥ 10; pre-v10 repos force one full re-parse in `indexFolder` (empty hash cache) because hash-skipped files would otherwise never backfill records and the rebuild would silently drop their edges. |
| 2026-08-26 | Phase 89: prune runs in `indexFolder` after discovery, and is SKIPPED when the file limit truncated discovery | Absence from a truncated listing is not evidence a file left the disk — pruning on it would delete live rows. When discovery is complete, DB-minus-disk rows get the full deletion treatment (symbols, both-direction edges, import records, FTS), which converges an in-place branch switch instead of accreting the union of branches. Worktree-per-branch stays the documented first-class pattern (path-keyed repoId — now stated as design intent, per the report). |
| 2026-08-26 | Phase 89: reserved namespaces filter at REGISTRATION (files) + short-circuit at resolve (specifiers); DI checks types pre-`bare()` | Registration-time exclusion (the go-resolver vendor-skip shape) keeps reserved-declaring shims out of every candidate path INCLUDING the basename/symbol-table fallbacks; the specifier short-circuit stops a reserved import from ever reaching those fallbacks and matching a same-named local symbol. In di-edges the check must run before `bare()` strips packages — the v1 plan's "DI unaffected" claim was wrong (a local `android.util.Log` shim would have provided bare `Log`), caught by gap-analysis-v2 and encoded as a test. Config list, `[]` = AOSP-fork opt-out. |
| 2026-08-26 | Phase 89: ONE shared test-path predicate (`src/core/test-paths.ts`), applied BEFORE `preferSameModule` in the resolver | Five private `isTestFile` copies already existed and the resolvers had none — adding a sixth (the naive Task-549 implementation) would have deepened the drift. Filter order matters: dropping test candidates AFTER the same-module preference could discard a legitimate main-source candidate displaced by a same-module test stub. The union predicate adds Gradle (`androidTest`, `testFixtures`) and .NET (`*.Tests/`) sets, so the `.cs` half of the declared-module family is covered too. |
| 2026-08-26 | Phase 89: `bootstrap-registry.ts` is THE registration list; CSS stays config-gated via a parameter | The four hand-kept lists had measurably drifted: run_purecontext carried 21 of 41 handlers (half the language surface in benchmark runs), run_benchmark registered spring-boot BEFORE android (violating the Phase-85 first-match routing rule), and main-vs-worker ADAPTER ORDER differed. One module, one order. The only intentional divergence — `indexing.cssVariables` gating the CSS handler in the MCP entry — became an explicit `registerStandardHandlers({cssVariables})` parameter instead of a fork of the list. Library surface = new `"./lib"` export (`bootstrapLibrary`); `.`/`bin` byte-identical. |
| 2026-08-26 | Phase 88: SQL handler accepts `@extschema@.` name prefixes | TimescaleDB (and PGXS extensions generally) declare their whole public API behind the psql substitution placeholder; the CREATE regex required the name to start with `\w` so every such statement silently failed — the single highest-leverage fix of the phase (timescaledb 0→60 P@1). The prefix is stripped by the existing `localName()`, so stored names stay bare. |
| 2026-08-26 | Phase 88: OpenAPI `findKeyOffset` gets a one-pass key-offset index; old per-key scan kept as fallback | The old "0 files indexed" report blamed a regex that does not exist; the true defect was O(keys × bytes) repeated full-buffer scans (6.2 s per 10 MB spec — hours on rest-api-description). The index does two O(bytes) regex passes (line-anchored keys; quoted keys anywhere, covering minified JSON) with incremental char→byte conversion only when the file is non-ASCII. A map miss falls back to the old scan, so correctness can only improve. |
| 2026-08-26 | Phase 88: hook/composable/component kinds index their kind word as FTS tokens; hook ↔ composable get BOTH tokens | Hook names never contain "hook", so hook-vocabulary queries could not retrieve them into the 200-candidate pool at all on 28k-symbol monorepos (novu — retrieval, not ranking, the Phase-71 lesson confirmed). Both tokens on both kinds because adapter routing on mixed monorepos tags React hooks as `composable` (a Vue/Svelte adapter claims the `.ts` files) and Vue queries say "composable" while React queries say "hook". Component kept separate. Isolation-verified as regression-free on nuxt/excalidraw/nestjs. |
| 2026-08-26 | Phase 88: benchmark harness imports production search helpers instead of duplicating them | The harness's dim2 path had silently drifted: it lacked the Phase-71 hook OR-fallback clause and passed no `RankOptions` — so the Phase-71 novu/mixed-monorepo/Java-Groovy/Angular ranking features were NEVER measured by any benchmark since. `hasReactHookQuery`/`detectMixedMonorepo`/`detectJavaGroovyMixed` are now exported from `search-symbols.ts` and imported by the harness; the same drift cannot recur. |
| 2026-08-26 | Phase 88: jenkins is a RANKING gap, not a coverage gap — fix = Java/Groovy-gated generic-verb identity scaling + camelCompound boost | The task predicted a symbol-coverage fix, but every "missed" ground-truth method was already indexed; replay showed generic single-verb methods (`run`, `execute`) taking full identityExact (+60) from one query word or its verb synonym while compound-named targets (`setResult` — every name part in the query) had no identity signal. identityExact ×⅓ for `GENERIC_METHOD_NAMES` + `camelCompoundBoost` +30, both under the isJavaGroovyMixed gate, which now auto-activates for Java-dominant pools (jenkins pools often contain zero .groovy files, so pool-based mixed detection missed them). Guards improved (gradle 2×, maven +4pp P@1). The un-gated TS instance of the same pattern (nestjs gt-03 `EmailService.send`) is documented and deferred — global formula changes are outside the phase boundary. |
| 2026-08-26 | Phase 88: OR-fallback fires on AND pools < 5 (was: only 0) | A 1–4-candidate AND pool almost always means low recall (jenkins "get the project that owns a build" had a 1-symbol AND pool and the target could only arrive via OR). AND hits still outrank merged OR hits (they match all terms), so precision cost is negligible; maven improved, nestjs A/B showed zero effect. Applied in production and harness together. |
| 2026-08-26 | Phase 88: Haskell kind hints honour the query's announced kind BOTH ways (+35 / −20), gated to .hs/.lhs | P@1=0 with decent P@3 was always a kind mismatch at rank 1: the type outranking the asked-for function via identityExact, or a spaced data-constructor/typeclass-instance symbol outranking the asked-for type via word overlap. Haskell ground-truth queries name the kind explicitly ("function that…", "record…", "sum type…"), so the hint is high-precision; the negative side is what actually flips rank 1. Spaced names ("ToHeaderValue PreferCount") are never search targets → −15. Extending the global kindHint table (whose comment already promises 'function' but never implemented it) is deferred — per-language gates only. |
| 2026-08-26 | Phase 88: Swift extension symbols get position-keyed IDs | `extension Foo` emits name=Foo kind=class, so same-file extensions collided with the primary declaration's `makeId(filePath, name, kind)` and INSERT OR REPLACE let the LAST extension overwrite the class row — destroying its doc comment (swift-nio's ChannelPipeline reduced to a "Swift extension:" stub; conceptual queries had nothing to match). IDs for extensions now include `#ext@<startByte>`; the name stays the bare type name for search. Re-index required to heal existing Swift indexes. |
| 2026-08-26 | Phase 88: ObjC handler normalizes GNUstep `GS_GENERIC_CLASS(Name, …)` to `Name` before interface matching | libs-base declares every generic Foundation class through the macro, so the macro token was captured as the class name and NSArray/NSString/NSDictionary were entirely absent from the index. Normalization also preserves the superclass when it is itself a macro call. Scores stay 4/8/8 — the remaining gap is conceptual-query vocabulary (GNUstep headers carry no Apple-doc-style text; JC also ~0) — recorded and stopped per the timebox rule. |
| 2026-08-26 | Phase 88: nuxt's 20/28/40→4/12/24 recorded as stale-baseline correction, NOT regressed and NOT "fixed" | Isolation run (pre-88 harness behavior + FTS tokens disabled + re-index) scores 4/8/20 on current code — the drop accrued between v1.7 and v1.20, exactly the measurement-scope shift Phase 75 predicted when `.vue` indexing was fixed (pool composition changed). Phase-88 changes are net +4pp on nuxt. Same discipline as the Phase-84 listmonk isolation run. Carried: a dedicated nuxt vocabulary pass. |
| 2026-08-26 | Phase 87: Rust module map from FILE LAYOUT under `src/`, not from parsing `mod` declarations | Layout and `mod` declarations agree in almost all real code (the task's sanctioned v1 simplification), and layout derivation needs zero extra parsing — the map builds from the files table alone. `src/a/b.rs` and `src/a/b/mod.rs` both register as `a::b` (2015+2018 layouts); `lib.rs`/`main.rs`/`src/bin/*` register at the crate root, so a crate with both lib and main over-approximates root imports to both (Phase 82 rule). `#[path]` overrides and macro-generated modules are documented limitations. Longest-prefix uses the Go rule — once a prefix matches, shorter prefixes are never tried (a miss means inline mod/unindexed, not a different module) — and the leaf symbol-table check is SCOPED to the resolved module's files (the task's "in that file"), never repo-wide: a repo-wide fallback for names like `new`/`Error` would flood the graph. |
| 2026-08-26 | Phase 87: bare-head resolution order = own crate's top-level modules FIRST, then workspace crate names | Rust 2018 uniform paths make `use foo::bar` valid for a top-level in-crate module `foo` (stabilized 1.32), so this is modern semantics, not 2015 compat — and it also covers 2015-edition code for free. The own-module check precedes the workspace-crate-name check because it is the more local binding; a genuine name clash is a compile error in real Rust anyway. Crate names come from a line-parsing `[package]` section walk (a regex broke on `authors = […]` appearing before `name`), dash→underscore normalized. A repo with no Cargo.toml falls back to the repo root as the crate dir so plain `src/` fixtures and vendored subtrees still resolve. |
| 2026-08-26 | Phase 87: Rust visibility — index EVERYTHING; `pub(crate)`-family → `'crate'`, no modifier → `'module'`; no up-front ranker penalty | Follows the Kotlin/C#/Go line: module-private items are visible to child modules and the same file, and the module sits inside the indexed unit, so skipping them produced false `no_match` → confident-wrong-answer chains. Rust has no true `private` keyword — skipping nothing is the honest choice, metadata does the filtering. The old gate (`startsWith('pub')`) already indexed `pub(crate)` silently; now it is recorded. The Phase-84-style rank penalty was NOT added up front; the agreed trigger (serde P@1 −11pp on re-run) then fired, so the existing `unexported` penalty (−20) was extended to `visibility:'module'` — `'crate'` stays unpenalized (crate-visible = legitimate API surface, and the old code already indexed `pub(crate)` without complaints). The penalty recovered serde to 20/23.3/33.3 and tokio P@1 to exactly the old 40% baseline; indexing was never reverted. |
| 2026-08-26 | Phase 86: five sibling resolver modules, NOT a parameterized `jvm-resolver` factory; dispatch = one extension→resolver Map | Only PHP resembles the JVM package+name shape — Haskell is exact-module, Elixir/Fortran are symbol-table lookups, Erlang is basename. Parameterizing `jvm-resolver` risked the byte-identical JVM/C# constraint for no shared code worth the risk; small sibling modules mirror the python/go precedent. The growing if-chain in `buildGraph` became `buildDispatch()` (extension → resolve fn, families export their extension sets) and `buildFamilyResolvers` a `FAMILY_DEFS` table — adding Rust in Phase 87 is one table row + one module. TS parity and bare-JvmResolver back-compat both kept. |
| 2026-08-26 | Phase 86: PHP resolves symbol-table FIRST, then declared namespace, then whole-namespace (fanout-capped), then composer PSR-4 | The PHP handler stores fully-qualified symbol names (`App\Http\UserController`), so an exact symbol match is the cheapest, most precise hit and covers multi-class files that basename matching misses. PSR-4 (composer.json `autoload` + `autoload-dev`, discovered by memoized ascent like go.mod) is deliberately LAST: it is a path-existence check that only fires when no declared-namespace row matched (headerless/pre-index files), so it can never override real namespace data. composer.json dirs double as module roots for same-module ambiguity preference (JVM Gradle/Maven rule). Backslash specifiers never touch path normalization — Windows stored paths also use backslashes, one shared helper would corrupt both. |
| 2026-08-26 | Phase 86: Erlang emits `-include`/`-include_lib` and resolves headers + modules by BASENAME; `:` checked before `/` | Module == file basename is the language rule, so basename lookup is exact, not heuristic (collisions → all candidates, Phase-82 rule). Include dirs are build configuration (rebar/erlang.mk `-I` flags) the indexer cannot see, so `.hrl` resolution also keys on basename — `include_lib`'s app-relative path still resolves for in-repo umbrella apps. Ordering bug caught by test: `util:go/0` contains a slash (arity), so the module:fun check must run before the path-literal check or every -import misroutes to the hrl branch. |
| 2026-08-26 | Phase 85: Compose/DI enrichment via `enrichMetadata` + a per-file facts cache — NOT spring-kotlin's merge-by-id override | The plan's merge-by-id mechanism replaces the handler's whole symbol record, clobbering real byte spans and signatures (spring-kotlin's classes have zero-length spans — an existing wart, not a precedent to copy). Instead `extractFrameworkSymbols` collects annotation facts per file into a bounded module-level cache and returns `[]`; `enrichMetadata` (React-adapter pattern) upgrades kind/attaches `frameworkMeta.di` with the id recomputed and spans preserved. Safe in workers: the cache is written and read synchronously within one `processFile` call (no macrotask can interleave between extract and enrich), and each worker has its own module instance. |
| 2026-08-26 | Phase 85: DI edges rebuilt repo-wide (`deleteEdgesByType('di')` + `buildDiEdges`) at both graph-build sites, gated on the android adapter | DI edges derive from the whole symbols table, not from the current batch's imports — an incremental per-file build would miss edges whose provider/consumer sits in an untouched file, and `deleteEdgesByFile` only clears the re-processed file's rows. DI edge counts are small, so delete-then-rebuild is cheap, idempotent, and keeps targeted re-index byte-identical to a full index. Classes with `@Inject` constructors count as providers of their own type (true Dagger semantics — constructor injection is the most common provision; without it most consumer edges would dangle). |
| 2026-08-26 | Phase 85: `find_cycles` excludes `edge_type:'di'` | Decided on fixture evidence as the plan required: Hilt's `@Binds` pattern makes the module provide the interface AND consume the implementation, so every binding yields a module ↔ impl edge pair — a structural 2-cycle BY DESIGN, not an architecture error. Blast radius / importers / coupling keep DI edges (that coupling is the feature); cycle detection keeps its import-cycle contract. |
| 2026-08-26 | Phase 85: `processWithAdapter` mirrors the normal handler path for regex-only handlers + `detect()` gates | Root-cause fix found by the e2e: the adapter path unconditionally called `parseFile`, which throws for `grammarPath() === null` handlers — so routing `AndroidManifest.xml` through the android adapter dropped the file entirely (xmlHandler is regex-only). No adapter had ever claimed a regex-handled file before, so the gap was invisible. Also honours handler `detect()` content gates in the adapter path (parity; no active adapter routes files to a detect-gated handler today, verified). |
| 2026-08-26 | Phase 85: `gradleModule` from the path convention (segments before the first `src/`), not from `settings.gradle` | `enrichMetadata` runs in worker threads that resolve adapters by name — state computed in `detect()` never reaches them (Phase-75 Nuxt lesson), and relative paths give no repo root to read `settings.gradle` from. The `src/` boundary is the Gradle layout invariant (`app/src/main/…` → `:app`, `feature/login/src/…` → `:feature:login`, root `src/` → `:`), needs zero I/O, and is deterministic across worker/sequential paths. `settings.gradle` stays the source of truth in the documented layer-config recipe (FRAMEWORK-ADAPTERS.md), where the agent — not the indexer — reads it. |
| 2026-08-26 | Phase 84: Python module identity derives from the **file path** (layout-convention resolver), no schema change | Unlike JVM/C#, Python has no declared package header — `a/b.py` IS `a.b`. The resolver registers each file under its repo-root dotted name plus a root-stripped name when the first-level dir is a source root rather than a package (`src/` always; any first-level dir without `__init__.py`, since a non-package dir is not importable). A first-level dir WITH `__init__.py` is part of the module path and is never stripped. Relative imports (`.`/`..pkg`) resolve by exact directory walk (highest-confidence edges, no index needed); `from a.b import c` prefers the submodule `a/b/c.py`, else the module file with a symbol-table tiebreak on ambiguity. v1 limitations documented: no `sys.path`, editable installs, or `pyproject` package-dir remapping. |
| 2026-08-26 | Phase 84: Go resolver = `go.mod` module table + exact package-directory expansion — deliberately NOT the JVM machinery | Go's language rules make it simpler: an import path maps to one directory, and every non-vendored `.go` file in that directory IS the package — edges to all of them is the true semantic, not over-approximation, so no fanout cap and no symbol-table fallback. Module discovery ascends from each indexed package dir, reading each `go.mod` once (memoized; `module` directive regex); nested modules = workspaces, longest prefix wins, `/v2` suffixes work for free. Once the longest prefix matches, shorter prefixes are never tried (a miss there means unindexed, not a different module). `_test.go` included (same package); no edges into `vendor/`; stdlib/third-party → no edge. |
| 2026-08-26 | Phase 84: `buildGraph` 4th param widened to `FamilyResolvers` (`{jvm?, python?, go?}`), bare `JvmResolver` still accepted; both index-manager sites use one `buildFamilyResolvers` helper | Task 519 allowed the refactor once three families made positional params ugly. Dispatch stays per-source-file-extension; each family's resolver is built only when the batch contains that family's files (zero cost on TS repos — throwing-stub regression guard still passes via the back-compat detection `typeof arg.resolve === 'function'`). Python's resolve() takes `importedNames` (from-imports need them); Go's does not. `buildFamilyResolvers` lives in `src/graph/family-resolvers.ts` so both `index-manager` sites share one decision point (build after persistence, Phase-82 rule). |
| 2026-08-26 | Phase 84: index unexported Go names (`frameworkMeta.visibility: 'unexported'`), exported names carry nothing | Same bug class as Kotlin `internal`/C# `internal`: unexported Go names are package-visible and the package sits inside the indexed unit, so skipping them produced false `no_match` → confident-wrong-answer chains. All six extraction sites (functions, methods, types, consts, vars, interface methods) now emit with visibility recorded; `get_public_api` is unaffected (it keys on the TS `export` keyword). The pre-agreed ranker lever (mild −20 penalty for `visibility:'unexported'`, below identityExact +40 so exact-name lookups still surface unexported symbols first) was added as a guard. **Attribution verified by an isolation run**: listmonk with the pre-84 handler on current code scores identically (4/16/40), so the listmonk drop vs the stale v1.2.0 baseline (28/60/72) is 0pp from Phase 84 — it accrued between v1.2→v1.16 and goes to the Phase 88 search-quality sweep. |
| 2026-08-26 | Phase 83: C# visibility respects the **asymmetric defaults** — modifier-less TYPE ⇒ internal ⇒ indexed; modifier-less MEMBER ⇒ private ⇒ skipped | Not a copy-paste of the Kotlin gate (Task-503 recipe): in C# a top-level type with no modifier is `internal` (assembly-visible — exactly the indexed unit, so it must be findable; dropping it explained the 8–12% benchmark floor), while a member with no modifier is `private`. Explicit `private`/`private protected` skipped everywhere; interface members keep the implicit-public bypass; non-public visibility recorded in `frameworkMeta.visibility` so API-surface tools can still filter. |
| 2026-08-26 | Phase 83: C# rides the Phase-82 resolver — extension-set + markers + fanout cap, NOT a new resolver; new `graph.maxWildcardFanout` (default 100) | The resolver already handled both shapes C# needs (bare-namespace → wildcard path; `X.Y.T` → longest-prefix + basename/symbol). Renamed to declared-module family (`DECLARED_MODULE_EXTENSIONS`, deprecated JVM aliases kept). `.csproj`/`.sln` have arbitrary basenames so module markers moved from `existsSync` of fixed names to one memoized `readdirSync` per dir (same cost profile). Every C# `using X.Y` imports a whole namespace, so package-wide expansions are capped at `maxWildcardFanout` files (deterministic sorted order, warn once per build, 0=uncapped) — applies to JVM wildcards too (shared code path). `extractPackage` stores only the OUTERMOST namespace for nested blocks (v1; symbol-table fallback covers the rest). |
| 2026-08-26 | Phase 82: JVM import resolution keys on the **declared `package` header** captured per file (schema v9 `files.declared_package`), path convention only as fallback | Kotlin does not require package = directory, so path guessing alone is wrong on real code. Capturing `package_header`/`package_declaration` per file (optional `extractPackage` on `LanguageHandler` — worker parity required, Phase 75 lesson) turns resolution into an exact lookup. The v9 index lives ONLY in the migration: a base-DDL `CREATE INDEX` on the new column crashes existing v8 DBs opened before migrations run. `upsertFile` is tri-state (`undefined`=preserve, `null`=clear) so hash-only upserts can never wipe the column. |
| 2026-08-26 | Phase 82: resolver seam stays **minimal** — one `JvmResolver` + extension dispatch in `buildGraph`, built per graph-build from the DB; no per-language resolver registry | The registry redesign is speculative until a third resolution family (Python/Go) actually lands; the JVM resolver being a thin DB consumer (files + symbols tables, built AFTER persistence at both `index-manager` sites) means targeted re-index and full index share one code path. Cross-module ambiguity prefers the importer's own Gradle/Maven module, else edges to ALL candidates — over-approximating is the correct failure mode for blast radius; picking one silently is not. TS path byte-identical, guarded by a throwing-stub test. |
| 2026-08-26 | Phase 82: index Kotlin `internal` (skip only `private`), record `frameworkMeta.visibility`; plus honesty signals (`limitReached`, `graphCoverage:'empty'`) | `internal` is module-visible and the module is exactly the indexed unit — dropping it made `search_symbols` return false `no_match` for the most-searched symbols (impl classes, DI modules), which AGENT_INSTRUCTIONS then hardens into a confident wrong answer. Same theme: a truncated index (10k limit) and an empty graph must be *visible* to the agent, not silent — `index_folder` now returns `limitReached`/`totalBeforeLimit`, and graph tools attach `graphCoverage:'empty'` on ≥20-file repos with 0 edges. All from the external Kotlin/Android evaluation (`dev-docs/purecontext-kotlin-support-request.md`). |
| 2026-06-28 | Phase 81: upgrade `get_task_context` **in place** with `mode:'flat'|'associative'` (default associative), not a sibling tool | The genuinely new value (active associative walk + evidence gap, from the MRAgent/MemR³ papers) is an *upgrade* to the existing single-shot pipeline, not a parallel surface — a sibling tool would split the discovery story and invite tool sprawl. `mode:'flat'` short-circuits Stage 1.5 to byte-identical pre-81 output (regression-guarded: flat omits `provenance`/`evidenceGaps`/`suggestedProbes`/`_meta.mode`), so existing consumers are unaffected while the default gets smarter. |
| 2026-06-28 | Phase 81: associative expansion is a **thin consumer** — `getContextBundle`+`getBlastRadius`+`getCoChange`, NOT `find_references` | Carried thin-consumer rule (Phase 77/79): `expandSeeds` composes existing graph engines; no graph logic re-implemented. Reverse edges use `getBlastRadius` (cheap dep_edges BFS) — `find_references` is a **full-repo content scan** (`getAllFilesWithContent` + regex per line), far too expensive to run per seed. Co-change gated on `countCommits>0 && signalQuality!=='low'` and memoized per filePath; ≤5 symbols per ≤5 partner files (mirrors `buildHistoricalNeighbors`). |
| 2026-06-28 | Phase 81: derive role/edge from the **graph**, not the AI's guess; agent is the **router** | Role now reflects the edge that surfaced a symbol (`imports`→dependency, `importedBy`→caller, `coChange`→historical, seed→primary); AI keeps only the prose reason (ContextRole widened with `caller`/`historical`). And no server-side reflective loop in v1 — one associative round returns the selection + `evidenceGaps` + `suggestedProbes`, and the harness's own LLM decides whether to call again. This protects the no-AI-key / low-token value (the metric the source article celebrates is *better* without a second LLM) and matches judgment-not-actuation. The no-AI fallback ranks by provenance, so the floor rises even with zero embeddings (pure SQL). |
| 2026-06-28 | Phase 80: the linchpin is a **single-file targeted re-index** (`index_file`), not a faster `index_folder` | Spike measured `index_folder` on govproxy (582 files): cold 14.7s, **no-op 11.5s** — it's discovery-bound (stats every file), so the change-amount barely moves it. Firing it after every edit would stall the agent ~11s/write. `index_file` reuses the existing core `reindexFiles` (skips discovery entirely) → O(one file). At task *start* `index_folder` is fine (amortized); only the per-edit loop was broken. The PostToolUse hook + a new `index-file` CLI subcommand now use the targeted path (full-index bootstrap on first sight). |
| 2026-06-28 | Phase 80: fix the recurring no-op re-parse churn at the source (always persist the file hash) | The spike found 27 files re-parsing on every no-op. Root cause: `indexFolder` (both parallel + sequential paths) `continue`d past `upsertFile`/`cache.set` for files yielding 0 symbols AND 0 imports → their hash was never stored → re-read+re-parsed every run. Now always persist (delete-then-upsert even when empty) → a true no-op reports `filesIndexed:0`. Existing `filesIndexed:toBe(0)` tests passed before only because their fixtures had no 0-symbol files. Also made `reindexFiles` clear stale rows when an edit empties a file (parity with a full index). |
| 2026-06-28 | Phase 80: greenfield consistency check is **structural-first** — no embedding provider required | The open question (does greenfield value need semantic search?) is resolved by building `check_consistency` on structural search alone (name-token Jaccard + signature), `mode:'structural'`. This makes the embedding question moot, matches the govproxy harness (no provider configured), folds in the Task-485 degrade, and works from file #2 onward (zero git history). Semantic dedup is a future optional enhancement, not a dependency. `signalQuality:'low'` on a sparse index suppresses dedup so it never invents "you already wrote this". |
| 2026-06-28 | Phase 80: orientation (Task 484) satisfied by composing existing `get_task_context`, not a new tool | `get_task_context` already turns task text → ranked relevant symbols + estimated files, and **degrades to FTS5 (structural) when no embeddings exist** — exactly the cold-start surface Task 484 specified. Building a near-duplicate would violate the thin-consumer rule (R5). Documented it as the harness orientation front door instead. Net new code avoided. |
| 2026-06-28 | Phase 80: gate envelope + `merge_readiness` are pure/thin — derivation lives in one module | `gate-envelope.ts` holds pure `gate{PrepareChange,VerifyChange,CompareChangeImpact,CheckConsistency}` + `worstGate`; each tool spreads `{gate,gateReasons,nextAction}` additively (existing `reasons` kept — envelope uses `gateReasons` to avoid clobber). `merge_readiness` calls `verify_change` + `compare_change_impact` handlers and set-combines (worst gate; coverage from verify's diff-scoped `coverageGapsRemaining`, not whole-repo `find_untested_symbols`). One place to fix the mapping; no analysis duplicated. |
| 2026-06-28 | Phase 80: Group D (govproxy wiring) NOT built — gated on explicit go-ahead | Groups A–C + E are PureContext-internal (server tools, core, config, docs, tests) and shippable as 1.12.0. Group D edits a *separate* repo (`d:\private\govproxy\govproxy`: skill `allowed-tools`, hooks, at-start indexing) and changes the user's working harness — reversible but outward-facing, so it waits for explicit instruction. |
| 2026-06-19 | Vue preprocessor: anchor the `<script>` OPEN tag to column 0 (`/^<script\b/m`) | `splitVueSFC` counted every `<script` / `</script>` in raw text to validate tag balance, then extracted with an unanchored regex. Real components tripped it: a regex literal in the script body (`/<script\b…<\/script>/`, TextEditor.vue) and JS strings building HTML with embedded, indented `<script>` tags whose closes are escaped `<\/script>` to survive Vue's own compiler (Sudoku.vue / CrosswordsGame.vue) → "N open but 1 close" ParseError → whole file skipped (0 symbols). Fix: a top-level SFC `<script>` block always starts at column 0, so anchor the OPEN match (count + extraction) to line start with the `m` flag; embedded `<script` (mid-line regex literals, indented HTML strings) is ignored. The CLOSE stays unanchored so single-line `<script>…</script>` blocks still balance. Escaped `<\/script>` never matches the plain close, so it's transparently skipped. The throw is kept (now col-0-based) for genuinely malformed SFCs — an *unescaped* inner `</script>` would break Vue's compiler too, so valid files never hit it. Verified on mondo2022: 4 components went 0 → 49 symbols, `errors: []`. +2 tests. |
| 2026-06-19 | Indexing perf: per-file git-metadata capture → **single repo-level pass** (`readRepoFileHistories`) | Diagnosed a real-world "indexing hangs 30+ min" on a 1.9 GB Nuxt repo (1,185 source files). Discovery + parsing + test-mapper finished in ~3 s; the rest was step 10f git capture. The old path called `readFileHistory` per file = **2 `git log --follow` spawns each** (history + `--oneline -n 501` count), 4-wide → ~2,370 git processes. The count call forced a full-history walk with rename detection for any file with <501 commits. On a fast SSD it was ~128 s; on a machine where every `git.exe` launch is AV-scanned it ballooned to 30+ min (each spawn bounded only by the 15 s timeout). Fix: `readRepoFileHistories(repoRoot)` does ONE `git log --name-only --format=<marker>%H\|%an\|%ae\|%at\|%s` pass, aggregating per-file (lastCommit, count capped 501, last-N history newest-first), written in one transaction. Result on the repro repo: git step **128 s → 0.9 s**, total **130 s → 3.7 s** (~35×), identical 3,074 symbols + 3,299 history rows. **Tradeoff:** the single pass drops `--follow`, so a renamed file's history only covers commits since its last rename — consistent with the Phase 76 co-change reader (`readRepoCommitFiles`), which made the same call. New config `git.fileHistoryDepth` (default 0 = full history; positive bound for giant monorepos). `readFileHistory` kept (tests/back-compat) but no longer used by the indexer. +10 tests, suite 6022 green. |
| 2026-06-14 | Phase 79: compete on *judgment, not actuation* — do NOT build `apply_rename`/`preview_refactoring`/runtime traces | jCodeMunch leads on refactoring *tool count* and the savings/jCodeMunch review showed token-savings is a parity feature it games harder ($360 "cost avoided" off the same inflated naive-read baseline PureContext's own counter uses). Chasing either is a losing, low-credibility game. In an agentic world the agent already has a file-write tool, so an MCP that *applies* edits is redundant — the value is telling the editing agent what's safe and what it forgot. The three Phase 79 tools are all read-only; edit actuation is a permanent product boundary, not a backlog item. |
| 2026-06-14 | Phase 79: `prepare_change`/`verify_change`/`compare_change_impact` are thin consumers — no synthesis logic in the tool layer | Carried from 77A's "build the engine where the input is objective, then wrap it." `prepare_change` feeds *predicted* symbols/files to `synthesizeChange`; `verify_change` reuses `analyze_diff` *verbatim* (calls its handler, parses the result) for the actual-side synthesis then only does set-difference reconciliation; neither re-implements diff parsing or synthesis. Enforced in review. Keeps the closed loop consistent and means a fix to the engine fixes all consumers. |
| 2026-06-14 | Phase 79: `verify_change` is the differentiator (plan-vs-actual reconciliation) and is **stateless** | jCodeMunch has stateful sessions but no reconciliation of the real diff against a prior prediction. `verify_change` computes `unaddressedCoChange` (predicted partner still untouched — the headline), `addressedCoChange`, `unplannedChanges` (scope creep), `coverageGapsRemaining`. Prediction is passed back **inline** (`predictedFilePaths`+`predictedCoChange` from `prepare_change`), NOT stored in a `change_predictions` table — protects the stateless-server value (Task 470 decision); add the table only on demonstrated need. Co-change reconciliation suppressed entirely when `signalQuality:'low'` (never invent "you forgot X" on thin history). |
| 2026-06-14 | Phase 79: architecture *regression* delta needs a stored before-graph → extend `get_architecture_snapshot`, don't checkout+reindex | `compare_change_impact` reports only what the change *introduced* (`newCycles`/`newLayerViolations` = set-difference of before vs current), distinct from 77A's current-state `architecturalFlags`. The snapshot's `metrics` JSON previously stored only `cycleCount` (a number) — useless for membership diffing. Extended `StoredMetrics` with `cycles` (membership) + `layerViolations` (additive to the JSON blob, no schema migration; old snapshots lack them → `no_baseline` graceful degrade). Compute helpers (`computeCurrentCycles`/`computeCurrentLayerViolations`) live in `compare-change-impact.ts` and are imported by the snapshot creator so before/after use the **identical** representation (Task 473: reuse snapshot, never checkout+reindex). |
| 2026-06-07 | Phase 77: extract `RiskContext` before composing risk over a diff (perf prerequisite) | `computeSymbolRisk` per call ran `getCommitsInWindow` (90d), `getCouplingMap` (whole repo), the full complexity-distribution query, AND scanned every file's `raw_content` for test refs — calling it once per changed symbol in a 30-symbol diff = O(symbols × repo). `buildRiskContext(db,repoId)` computes all repo-wide distributions + the afferent-coupling lookup + the test-file content set ONCE; `computeSymbolRiskWithContext` is pure scoring against it (plus a per-symbol reverse blast radius). `computeSymbolRisk` kept as a thin wrapper with **byte-identical output** (regression-guarded by a test asserting single-call ≡ context path, and a query-count spy proving distributions build once for N symbols). Also memoizes `getCoChange` by file path on the context (many changed symbols share a file). |
| 2026-06-07 | Phase 77: synthesis logic lives in `change-synthesis.ts`, NOT inside `analyze_diff` | The genuinely new value is orchestration + one reviewer instinct (absence-of-co-change). Phase B's pre-edit `prepare_change` must reuse the EXACT same engine, so the fusion (`synthesizeChange`) is a pure, MCP-free module; `analyze_diff` (post-edit) and later `prepare_change` (pre-edit) are both thin consumers. Build the engine where the input is objective and testable (a real diff is ground truth), then wrap it in the fuzzier pre-edit surface — order matters. Enforced in review: no synthesis logic duplicated in the tool layer. |
| 2026-06-07 | Phase 77: `missingCoChange` is the headline signal — and is suppressed on low signal | The static import graph cannot see temporal coupling. "You touched refundService but ledgerService historically moves with it — and it's not in this diff" = co-change partners with `confidence ≥ threshold` (default 0.4) minus files already in the diff, minus test files (those route to `recommendedTests`). To avoid manufacturing warnings on shallow/squashed histories, the whole section is **empty + suppressed when `signalQuality==='low'`** (co-change window < 20 commits) — verified by a thin-history test and the live smoke (dropping `commit_files` ≡ `coChangeDepth=0` → low + empty, no crash). |
| 2026-06-07 | Phase 77: architectural **flags**, not **regressions introduced** | True before/after architecture regression (a NEW cycle / NEW cross-layer edge introduced *by* the diff) needs a before/after graph delta — that's `compare_change_impact`, deliberately deferred. Phase A ships the honest, testable subset: `architecturalFlags` = "do the changed files *currently* sit on an import cycle (`findCycles` over the live adjacency, filtered to cycles containing a changed file) / cross a layer boundary (`assignLayer`/`isAllowed` over edges touching a changed file)." Labeled as flags throughout; never claims the diff caused them. |
| 2026-06-07 | Phase 77: enriched `analyze_diff` is fully backward-compatible | All new output fields are additive; `reviewPriority` keeps the same `low|medium|high|critical` enum (smarter derivation: folds in aggregate risk band + coverage-gap count alongside the original signature-break/blast signal). Four section toggles (`includeRisk`/`includeCoChangeGaps`/`includeTests`/`includeArchitectureFlags`) default ON (the point of the tool) but switch off for cheap runs; with all off the output reduces to the pre-Phase-77 shape exactly (verified by test). Existing CI/JSON consumers unaffected. |
| 2026-06-04 | Phase 76: dedicated `commit_files` table (not `git_metadata`) for co-change | `git_metadata` stores commits per-file but capture is `readFileHistory(limit=10)` — only the last 10 commits/file via N per-file `git log --follow` calls, so a shared commit is recorded only if it falls in BOTH files' last-10 window → systematic undercount, recency-skewed. New `commit_files(repo_id, commit_sha, file_path, commit_date)` populated by ONE repo-level `git log --no-merges --name-only -n N` (commit→files natively, far deeper + cheaper). Co-change of two files = self-join on `commit_sha`. Schema v7→v8 additive (CO_CHANGE_DDL, IF NOT EXISTS); symbol-history capture untouched. Gated on `git.coChangeDepth > 0` (default 300; 0 = no extra git call, byte-identical). |
| 2026-06-04 | Phase 76: explainable association metrics + mega-commit handling | Generic "churn dashboards" present raw counts. `getCoChange` returns support (shared commits), confidence (support/commits(target) — directional A→B), lift (support×N/(commits(A)×commits(B))). Noise control: exclude commits touching > `megaCommitThreshold` (default 30) files (reformat/lockfile/codemod sweeps); down-weight each retained shared commit by `1/(k−1)` so a 25-file commit contributes far less than a focused 2-file one (ranks by `weightedSupport`); `minSupport` floor (default 2) drops coincidences; `signalQuality:"low"` when commits(target)<5 or window<20 rather than overstating weak ratios. |
| 2026-06-04 | Phase 76: composite `get_symbol_risk` — repo-relative midrank percentile | Fuses churn (90d) / centrality (afferent coupling + reverse blast radius) / complexity / test-gap / co-change spread into one 0–100 banded verdict. Each factor normalized **repo-relative** so scores compare within a repo and aren't dominated by absolute LOC. Used **midrank** percentile `(less + 0.5·equal)/N` not fraction-`<=`: the latter inflated ties at the repo minimum (a simple symbol jumped to ~0.9 complexity because most symbols also had cc=1). Weights from `risk.weights` (tunable, not magic constants). Always returns `factors`+`reasons[]` — never a black-box number. NO author/productivity metrics (product boundary). |
| 2026-06-04 | Phase 76: guardrail surfaces are bundle + opt-in flag, NOT plan_turn | PureContext has no `plan_turn` (that's a jCodeMunch tool). Risk reaches agents via `get_context_bundle.historicalNeighbors` (co-changing files the import graph can't see; empty + byte-identical when no co-change data) and an opt-in `includeRisk` flag on `search_symbols`/`get_symbol_source` (compact `{band,riskScore}`, default off so no token/perf cost). Full breakdown stays in `get_symbol_risk`. |
| 2026-06-01 | Phase 75: Svelte + Astro adapters (new SFC support) | No `.svelte`/`.astro` support existed. Added `svelteAdapter` (`.svelte`: `splitSvelteSFC` extracts `<script>`/`<script context="module">`, component symbol, `useXxx`→composable) and `astroAdapter` (`.astro`: `splitAstroSFC` extracts leading `---` frontmatter as a TS block, component symbol). Modeled on the Vue adapter. Shared `src/adapters/detect-utils.ts` provides bounded recursive monorepo detection (`scanForFramework`, `pkgDepMatches`, `toPascalCase`); Vue/Nuxt predate it and keep inline copies (left untouched to avoid risk). Astro frontmatter fence must be leading non-whitespace content — a `---` later in markup is not treated as frontmatter; unterminated fence throws ParseError. |
| 2026-06-01 | Phase 75: watcher + remote-index union adapter extensions; harness registers adapters | Same bug class as the `indexFolder`/`discoverAdapters` root cause: `file-watcher.ts` and `index-repo.ts` gated on `getSupportedExtensions()` (handlers only), so `.vue`/`.svelte`/`.astro` were filtered out (watcher ignored SFC edits; remote indexing dropped SFC blobs). Fix: union `getAdapterExtensions(getRegisteredAdapters())` (static union of all registered adapters — not detect()-filtered, since these paths lack a reliable local FS to detect against; over-inclusion is harmless because `processFile` gates on `fileFilter` and `indexFolder` re-detects). Benchmark harness (`run_benchmark.ts` + `run_purecontext.ts`) imported 40 handlers but 0 adapters → never indexed `.vue`; now imports all adapters for self-registration so the harness matches production. Vue/Nuxt benchmark numbers will shift on next run (measurement-scope correction). |
| 2026-06-01 | indexFolder now auto-discovers adapters when none supplied (ROOT CAUSE of ".vue not indexed") | `index_folder` MCP tool + benchmark harness call `indexFolder()` without `options.adapters`, and `indexFolder` defaulted to `[]` — it never called `discoverAdapters()`. Result: adapter-only extensions (`.vue`) were never added to the discovery allowlist, so `.vue` files were silently skipped and Vue SFC `<script>`/component extraction never ran (language handlers still covered `.ts`/`.tsx`, which is why NestJS/React still scored). Fix: `const adapters = options.adapters ?? await discoverAdapters(absRoot, { adapters: getConfig().adapters })`. Adapters self-register in the main process (src/index.ts) and workers (indexing-worker.ts). Backward-compatible: explicit `options.adapters` and processes that don't import adapter modules behave as before. Verified end-to-end on a real Vue 2 repo: 0 → 255 `.vue` files / 255 `component` symbols indexed. The earlier monorepo `detect()` fix was necessary but not sufficient — the pipeline never invoked detection at all. |
| 2026-06-01 | Vue/Nuxt adapter monorepo detection — bounded recursive scan | Both adapters' `detect()` only checked the indexed root (Vue: root `package.json` + `./`, `./src`, `./components`; Nuxt: `nuxt.config.*` at root). In multi-technology repos the framework app lives in a subdirectory (`frontend/`, `apps/web/`), so detection failed → Vue's `.vue` extension was never added to the discovery allowlist → zero Vue symbols. Added a bounded recursive scan (depth ≤ 6, ≤ 2000 dirs, skips `node_modules`/`.git`/`dist`/`.nuxt`/etc., does not follow symlinks): Vue scans for a `.vue` file or nested `package.json` declaring vue/@vue/*; Nuxt scans for a nested `nuxt.config.{ts,mts,js,mjs}`. Root fast-path preserved. Version 1.5.2→1.6.0. |
| 2026-06-01 | Nuxt nested app-root path resolution — stateless `toNuxtRelative()` | After Nuxt detection fix, route/plugin/middleware extraction + page/composable enrichment still failed for nested apps: `getCategory`/`deriveServerRoute`/`derivePageRoutePath`/`enrichMetadata` keyed off paths relative to the Nuxt app root (`server/api/...`), but monorepo paths arrive repo-relative (`apps/web/server/api/...`). Added `toNuxtRelative()` that returns the path from the first recognized app-root boundary segment (`server`/`plugins`/`middleware`/`composables`/`pages`); `getCategory` split into `categoryOf(rel)` + wrapper. Must be stateless (cannot cache app root in `detect()`) because the parallel worker pool resolves adapters from its own registry — main-thread state never reaches workers. `server` is a boundary segment so `server/plugins/foo.ts` resolves within the `server/` tree (correctly *not* an app-root plugin). Stored symbol `filePath` stays repo-relative so files can still be opened. Caveat: heuristic — a non-Nuxt dir literally named `server`/`pages`/etc. could match, low-risk since the adapter only runs after `detect()` confirms Nuxt. |
| 2026-05-22 | Phase 73: Perl/R package context boost — PACKAGE_SEGMENT_STOPWORDS + reduced multiplier | catalyst-runtime regression: package context boost added `+8*overlap` for Catalyst:: namespace symbols when query contains 'catalyst', causing TestApp:: and Catalyst:: wrapper classes to outscore bare functions like `uri_for`. Fix: (1) PACKAGE_SEGMENT_STOPWORDS filters generic MVC words (controller, action, model, view, app, core, etc.) from package name token overlap; (2) multiplier reduced to `overlap >= 2 ? (overlap-1)*8+4 : 4` (single match = 4, was 8). catalyst P@3+4pp, R@5+8pp; P@1 still regressed 20%→12% because `uri_for` is not retrieved by FTS at all ('for' is a stop word). |
| 2026-05-22 | Phase 73: Perl t/lib/ fixture penalty −25 | TestApp::Controller::Action symbols in `t/lib/` are test fixtures, not library API. They outscore Catalyst core classes because their names contain query words ('action', 'controller') multiple times. Added −25 library penalty for `.pm`/`.pl` symbols whose `filePath` starts with `t/lib/` or contains `/t/lib/`. Path normalized to forward slash + lowercase before check; covers both Windows backslash and relative paths. |
| 2026-05-22 | Phase 73: Interceptor boost +15→+30 | NestJS interceptor middleware symbols were being outranked by *Service kindBoost (+30) symbols on queries about interceptors. Doubling the interceptor boost to +30 puts them on equal footing with services when the query explicitly mentions interceptor behavior. |
| 2026-05-22 | Phase 73: Rendering REPO_PATTERN + 'draw' | excalidraw uses `draw*` function names (drawElement, drawScene) that benefit from rendering-domain compound synonyms. Added 'draw' to RENDERING_REPO_PATTERN regex so excalidraw repos get rendering-specific synonym expansion. |
| 2026-05-22 | Phase 73: Compound underscore boost +30 | Underscore-named functions (e.g., `dispatch_action`, `build_request`) have all their parts in query words but don't get identityExact because the full string doesn't match. Added +30 boost when ALL `_`-split name parts (excluding STOP_WORDS) are present in the query word set. Does not fire for 'for' (stop word) — limitation for `uri_for`. |
| 2026-05-22 | Phase 73: Single-token exact boost (+50/+40) | Single-word queries that exactly match a symbol's full name or bare (last `::` segment) name were not reliably scoring highest. Added +50 for exact full-name match and +40 for exact bare-name match in single-token query mode; guards against over-firing by requiring single-token context. |
| 2026-05-22 | Phase 72: Erlang bare names — store `start_link` not `start_link/3` | rabbitmq/emqx ground truth uses `name/arity` format but FTS5 tokenizes `start_link/3` as a unit; bare name greatly improves recall. Arity uniqueness preserved via `frameworkMeta.arity`; ID still uses arity-qualified key to avoid collisions between `greet/0` and `greet/1`. Module name injected as FTS token (raw + underscore-split) for `rabbit_channel:start_link` queries. Harness extended with arity-suffix stripping for backward compat. rabbitmq 0%→36%, emqx 0%→28%. |
| 2026-05-22 | Phase 72: Proto serviceName FTS token — inject service name into method BM25 weight | googleapis proto methods named `Spanner.Read` — queries for "Spanner streaming read" need to match both the service and the method name. Added `frameworkMeta.serviceName` to RPC symbols; `buildFtsContent` injects it as an extra token. Slight P@1 regression in googleapis (service class vs method ranking conflict for bare service-name queries) — acceptable trade-off vs recall improvement. |
| 2026-05-22 | Phase 72: Neovim C-API Lua alias — `vim.api.nvim_*` FTS token on C functions | Neovim Lua users call `vim.api.nvim_open_win()` but C implementation is `nvim_open_win` — queries never matched. Added `maybeNvimLuaAlias()` to C handler; fires for `nvim_[a-z_]*` functions in `/nvim/` paths. FTS alias resolves the language barrier. neovim 24%→36% P@1 (PC beats JC). |
| 2026-05-22 | Phase 72: Groovy source boost +10 for mixed Java+Groovy repos | gradle/groovy expected symbols are all in Groovy files but compete with Java classes; +10 preferentially surfaces .groovy results in `detectJavaGroovyMixed` repos. gradle unchanged (FTS retrieval gap is the bottleneck, not ranking); groovy minor R@5 regression (1 query) likely from re-index delta not the boost. |
| 2026-05-22 | Phase 71: Library path extensions (engine, erts, contrib) | flutter C++ engine/ (40k symbols) pollutes Dart widget queries; otp erts/ pollutes Erlang stdlib queries; contrib/ inflates scientific computing symbol counts. Added as directory-segment penalties (-35). Also added /lib/wx/, /blas/, /lapack/ as multi-segment substring penalties. `unstable-core-do-not-import/` was added then removed — tRPC's canonical core API lives in that path; penalizing it blocks ProcedureBuilder.* expected symbols (task spec was incorrect). |
| 2026-05-22 | Phase 71: Java/Groovy core-path boost and plugin-path penalty | jenkins 4%/8%/16% — generic method names (getDuration, isBuilding) appear in hundreds of plugin classes. Added +15 boost for `/core/src/main/java/` paths and -35 for `/plugins/`/`/plugin/` paths, domain-gated to java/groovy repos. Result: jenkins 4%/8%/12% (slight R@5 regression — plugin penalty demoted a false-positive bare-name match that the harness had been counting as a hit). Core gap: PC indexes 11k jenkins symbols vs JC 23k — a coverage gap, not purely a ranking gap. |
| 2026-05-22 | Phase 71: Frontend path boost + use*/hook OR-fallback | novu 0%/0%/0% — 28k NestJS backend symbols drown React hooks in apps/dashboard/. Added mixed-monorepo detection (checks both frontend and backend app dirs), +20 path boost for dashboard/web/frontend app paths when query has hook/component vocabulary, and OR-fallback when AND pool lacks use[A-Z] symbols. Result: novu unchanged — hook symbols not entering the 200-candidate FTS pool at all; path boosting can't help before retrieval. |
| 2026-05-22 | Phase 71: Path proximity boost for same-name symbols | godot/home-manager — `spawn_count` in multiple demo files, `programs` in thousands of Nix modules: identical names return arbitrary result. Added +5/token path-proximity boost when ≥3 pool symbols share a name and the file path tokens overlap with query words. Common path segments (src/lib/app/core/main) excluded. Not measurable for godot (empty index); home-manager unchanged (query tokens don't overlap with Nix module file names). |
| 2026-05-22 | Phase 70: ObjC handler full extraction | Existing handler registered but missing `.h` extension, category naming, full selector building, `property` kind. Added ObjC detection guard (check first 16KB for `@interface`/`@protocol` before processing `.h` files), `ClassName+CategoryName` for named categories, `classExtension: true` for anonymous categories, full selector construction (`setObject:forKey:`), property kind changed from `const` to `property`. libs-base 0%→4%, vlc-ios 0%→12%, SparkleShare-iOS 0%→12% (JC had 16%). |
| 2026-05-22 | Phase 70: HCL handler (new, regex-only) | terraform-aws-eks and terraform-aws-components score 0%. Implemented regex-based HCL handler extracting `variable`→const, `output`→const, `resource`→class (`type.name`), `data`→const, `module`→class, `provider`→const, `locals` items→const. Naming uses `var.name`/`output.name`/`module.name`/`local.name` prefixes matching Terraform reference syntax (ground truth expectations). Registered in both `src/index.ts` and `indexing-worker.ts`. Must also register in `benchmarks/harness/run_benchmark.ts` (three separate handler registries). terraform-aws-eks 0%→84%, terraform-aws-components 0%→68%. |
| 2026-05-22 | Phase 70: Angular HTML handler (new, regex-only) | Angular `.html` templates invisible to search. Added detection guard (sibling `.component.ts` OR Angular markers/event binding pattern in first 4KB). Extracts component selectors (kebab-case multi-segment), structural directives (`*ngIf`, `*ngFor`), control flow (`@if`, `@for`), event bindings (`(click)="handler"`), template refs (`#userInput`), routerLink. |
| 2026-05-22 | Phase 70: Extensionless file discovery (opt-in → automatic) | dokku plugin functions live in extensionless `plugins/*/functions` files. Initial Phase 70 implementation required `extensionlessFilenames?: string[]` opt-in. During benchmark, harness called `indexFolder` without the parameter so dokku stayed at 0%. Changed to automatic: all extensionless files included when no allowlist provided; shebang detection in file-processor routes to bash handler or returns 0 symbols for non-bash files. dokku 0%→4%/40%/60%. |
| 2026-05-22 | Phase 70: XML root-element disambiguation | Maven 30+ modules each have `pom.xml` with `<project>` root — FTS returns arbitrary one at rank 1, blocking 21/25 queries. `disambiguateXmlName()` appends `@module` when (a) element depth ≤ 2, (b) file is in a subdirectory (parts.length ≥ 2), (c) disambiguator differs from tag. Generic stems (pom, index, config, settings, default) use parent dir name; others use file stem. Bare tag name stored in `bodySnippet` for FTS fallback. |
| 2026-05-22 | Phase 70: XML `@` disambiguation — ranker + harness end-to-end fix | After `project@maven-core` names were stored, three further fixes needed: (1) `splitNameParts` split regex updated from `[\\:.]+` to `[\\:.@]+` so `project@maven-core` tokenizes to `["project","maven","core"]` for word-overlap scoring; (2) `identityExact` boost: added `bareTagName` for the `@`-prefixed part so query word `project` triggers identityExact on `project@maven-core`; (3) `buildFtsContent`: standalone tag name token added for BM25 weight; (4) harness: `@` prefix match so `project@maven-core` matches ground truth `project`. Without these, maven went from 12% to 8% after XML rename, then back to 48% after all four fixes. |
| 2026-05-22 | Batch 5: 7 new benchmark projects (Phases 63/67) | OpenAPI/XML (maven, stripe-openapi, kubernetes-openapi, rest-api-description) + SQL (postgrest-sql, jaffle-shop, timescaledb). PC wins 4/7 P@1. P0 gaps: (1) OpenAPI `\w+` regex misses hyphenated names — rest-api-description 0% (fix: `[\w-]+`); (2) SQL handler captures schema prefix as function name — timescaledb 0% sql-kind (fix: optional schema prefix regex); (3) XML root element name collision across Maven modules (fix: file-path in symbol name). Cumulative: PC 42/67 P@1. Total benchmarked: 79 projects. |
| 2026-05-22 | Batch 4: 15 new benchmark projects (Phases 60/61/62) | Terraform/Nix, Lua/GDScript, Protobuf/GraphQL groups complete. PC wins 11/15 P@1 (JC wins terraform-aws-eks 12%, terraform-aws-components 4%, neovim 32%, godot-demo-projects 40%). Envoy 56%/64%/72% vs JC 0%. P0 gap: no HCL handler — 0 Terraform symbols. Cumulative: PC 38/60 P@1. Total benchmarked: 72 projects. Version bumped to 1.5.0. |
| 2026-05-22 | Batch 4: HCL handler identified as P0 gap | terraform-aws-eks and terraform-aws-components both score 0%/0%/0%. PC indexes 0 files because .tf/.tfvars files have no handler. JC uses heuristic text extraction for HCL. Fix: implement HCL handler with tree-sitter-hcl WASM grammar, extracting variable/output/resource/data/module/locals blocks. |
| 2026-05-21 | Batch 3: Groovy/Java core-path boost identified as P0 gap | jenkins benchmark shows JC wins 32%/48%/56% vs PC 4%/8%/16%. Root cause: Java bare method names (`isBuilding`, `getDuration`) appear in hundreds of plugin classes; PC returns plugin implementations before `core/src/main/java/` methods. Fix: apply negative library-path penalty to `plugin/`, `vendor/`, `ext/` paths in Java repos, mirroring existing `vendor/node_modules/` penalty. |
| 2026-05-21 | Batch 3: R/Perl/Groovy ground truth — 9 new projects | Phase 58 (R+Perl) and Phase 64 (Groovy) complete. 9 new benchmark projects. PC wins 5/9 P@1; JC wins 1 (jenkins); 3 ties. Cumulative: PC 27/45 P@1 head-to-head. Total benchmarked: 56 projects. |
| 2026-05-21 | Batch 2: Harness dot-qualified name matching | Fortran/Elixir handlers store module-qualified names (`radiation_cloud.allocate_cloud_arrays`). Ground truth uses bare names. Harness now accepts a match when `name.split('.').pop() === expected` — mirrors the existing `::` C++ suffix match. Only fires for bare (non-qualified) expected symbols. |
| 2026-05-21 | Batch 2: case-insensitive extension in `file-discovery.ts` | `.F90` files not discovered because `entry.name.slice(dot)` returns `.F90` (original case) but `extensions` list has `.f90` (lowercase). Fixed: `if (!extensions.includes(ext.toLowerCase())) continue`. |
| 2026-05-21 | Batch 2: case-insensitive step-3b filter in `index-manager.ts` | After file-discovery finds `.F90` files, step 3b re-filters with `supportedExts.has(ext)` where `ext` was still uppercase. Fixed: `df.path.slice(dot).toLowerCase()` before set lookup. Without this, `.F90` files were discovered but silently dropped before reaching workers. |
| 2026-05-21 | Batch 2: 5 missing handlers in `indexing-worker.ts` | Workers have their own module registry (no shared state). fortranHandler, scssHandler, lessHandler, cssHandler, objectiveCHandler were not registered in `indexing-worker.ts` — files with those extensions reached workers but got 0 symbols. Added all 5 missing `registerHandler` calls. |
| 2026-05-21 | Batch 2: directory trailing-slash in `file-discovery.ts` | `ignore` npm package: `ig.ignores('deps/rabbit')` returns `true` even when `!/deps/rabbit/` negation is present, because the no-slash check matches `/deps/*` pattern. Fix: `const checkPath = entry.isDirectory() ? relPath + '/' : relPath` — appending `/` activates the directory-level negation logic in `ignore`. Fixed RabbitMQ `deps/` traversal. |
| 2026-05-20 | Phase 52: RUST_ONLY_SYNONYMS domain restriction | `future→poll`, `spawn→tokio/task`, `concurrent→parallel`, and serde synonyms now only fire when domain='rust'. Without scoping, `future→poll` caused `FutureBase::poll` to outscore `folly::Future` in C++ repos, blocking folly P@1. Matches the existing `RENDERING_ONLY_SYNONYMS` pattern. |
| 2026-05-20 | Phase 52: C++ bare local name in FTS content | `buildFtsContent` now appends the bare local name (`Future`) for `::` qualified symbols (`folly::Future`). Gives the local name a dedicated FTS5 token with boosted BM25 weight via repetition, improving single-word C++ queries. |
| 2026-05-20 | Phase 52: Harness qualified-name matching | Benchmark harness now accepts `folly::Future` as a match when ground truth expects bare `Future`. Uses `name.split('::').pop()` suffix comparison; only fires for bare (non-qualified) expected symbols to avoid false positives. |
| 2026-05-20 | Phase 52: Windows path-case fix in harness | `computeRepoId` is SHA-256 based and case-sensitive. Harness used `computeRepoId(repoPath)` (lowercase `d:/`) while `indexFolder` used `computeRepoId(resolve(rootPath))` (uppercase `D:/`), producing different hashes. Fix: use `indexResult.repoId` from the indexer rather than recomputing. |
| 2026-05-20 | Phase 52: test-mapper local try-catch | `buildTestMappings` now catches `writeAll()` failures locally and returns 0 rather than propagating. Prevents FK constraint errors in the test-mapper transaction from blocking subsequent FTS index population. |
| 2026-05-20 | Phase 52: C# interface member extraction | `extractMembers` adds `isInterface = typeNode.type === 'interface_declaration'` guard to skip visibility check for interface members (implicitly public in C#). Also fixes method name extraction: `methodName()` helper uses `findLast` before `parameter_list` to avoid returning the return type (first identifier) instead of the method name. Event field declarations (`event_field_declaration`) added as `property` kind. |
| 2026-05-20 | Phase 51: hasCppStyleMethods guard on class injection | Class-type secondary FTS injection (to fix C++ 0% P@1) was scoped to repos where method symbols contain `::` in their name. Prevents regression in Ruby/Rails repos (brew, mastodon, rails, discourse) where single-word class names would be outranked by compound injected symbols. |
| 2026-05-20 | Phase 51: Ruby DSL extraction (associations + callbacks) | Ruby handler now extracts `has_many`, `belongs_to`, `has_one`, `has_and_belongs_to_many`, `before_action`, `after_action`, `validates`, `scope` class macros as `property` symbols with DSL kind metadata. Improves brew/rails/mastodon symbol counts and search relevance for ActiveRecord model queries. |
| 2026-05-20 | Phase 51: Rust cfg frameworkMeta + cfgFilter param | Rust symbols annotated with `#[cfg(...)]` attributes now carry `frameworkMeta.cfgAttributes` array. `search_symbols` accepts a new `cfgFilter` string that restricts results to symbols whose cfg attributes match the filter string. |
| 2026-05-20 | Phase 51: get_lexical_scope_matches + trace_invocation_chain | Two new MCP tools: `get_lexical_scope_matches` returns all symbols accessible from a given file+line (local scope, imports, module exports); `trace_invocation_chain` follows call edges from a symbol N hops deep and returns the linearised call path. |
| 2026-05-20 | Phase 50: identityExact scaled for data kinds | Const/type/interface/enum/property symbols now get identityExact=40/N (min 10) in multi-word queries. Prevents STRIPE const or Subscribers struct from dominating when mentioned as context in a longer query. BM25 cap (30% when topBase≥80) kept. Fixes nestjs 76%→84% without breaking listmonk (28%). |
| 2026-05-20 | Phase 50: Multi-IDE install command | `npx purecontext-mcp install <tool|all>` injects PureContext workflow into Cursor (.cursor/rules/purecontext.mdc), Windsurf (.windsurfrules), Continue (.continue/config.json systemMessage), Cline (.clinerules), Roo Code (.roo/rules-code.md), VS Code (.github/copilot-instructions.md), Claude Desktop (platform config). All writers are idempotent via HTML markers. |
| 2026-05-19 | Phase 48: Rendering synonyms scoped via RENDERING_ONLY_SYNONYMS | Added set of rendering-only synonym tokens; expandVerbSynonyms/preprocessQuery/toOrFallbackQuery/rankSymbols accept optional `domain` param; search-symbols.ts detects rendering repos by name pattern (mitsuba, render, shader, etc.). Fixes nuxt/airodump/origamicms-frontend regressions. |
| 2026-05-19 | Phase 48: Claude Code hooks in Node.js | Cross-platform (Windows/Linux/macOS) without bash/PS1 split — Node is already a hard dependency. Three hooks: PostToolUse index hook, PreCompact snapshot, Edit Guard (soft, never blocks). |
| 2026-05-19 | Phase 48: Negative evidence in `search_symbols` | When 0 results after all fallbacks, return `verdict: "no_match"` to stop agents from re-searching with variant queries. |
| 2026-05-19 | `AGENT_REFERENCE.md` in project root | Full tool reference, navigation patterns, known limitations moved out of global CLAUDE.md. Always-on instructions trimmed to ~80 lines; reference loaded on demand. |
| 2026-05-18 | Phase 47: Java bare method names + C++ template class extraction | fleetdirect-android 0%: Java methods used qualified names. mitsuba3 0%: tree-sitter-cpp misparsed `class MI_EXPORT_LIB ClassName` as `function_definition`. Fix: detect misparse pattern, emit class symbol, walk body. |
| 2026-05-18 | Phase 46: Go/Rust bare method names + identity-exact boost | PC stored receiver-qualified names (`Manager.PushCampaignMessage`) but ground truth uses bare names. Identity-exact +40 boost mirrors JC's Identity channel (weight=2.0). |
| 2026-05-17 | Stylesheet handler: regex-only (no WASM) | SCSS/LESS/CSS handlers use regex extraction — no tree-sitter grammars available. Only named reusable constructs indexed (mixins, variables, functions); plain selectors excluded. |

---

## Quick Commands

```bash
# Development
npm run build          # Compile TypeScript
npm run dev            # Watch mode
npm run test           # Run test suite
npm run lint           # ESLint

# CLI
npx purecontext-mcp                    # Start MCP server (stdio)
npx purecontext-mcp config --init      # Generate default config
npx purecontext-mcp config --check     # Validate config + prerequisites
npx purecontext-mcp hooks --install               # Install Claude Code hooks
npx purecontext-mcp install all                   # Auto-detect IDEs, prompts for scope
npx purecontext-mcp install all --scope=global    # Install globally without prompt
npx purecontext-mcp install cursor --scope=local  # Install for a specific IDE

# Claude Code integration
claude mcp add purecontext-mcp npx purecontext-mcp
```

<!-- purecontext-mcp-start -->
## PureContext MCP — Code Navigation & Safe Change

Prefer PureContext MCP tools for code navigation and impact checks — one indexed lookup usually replaces several whole-file reads. These are defaults, not hard rules: fall back to plain file reads when the index lacks what you need or you already know exactly where to look.

### Recommended workflow

1. `list_repos()` once per session → `repoId` (all tools need it). Not indexed? `index_folder({ path })`.
2. Orient: `get_task_context({ repoId, task })` → the most relevant symbols + files, walked over the real dependency/co-change graph. Empty result → name a real symbol, or use `search_symbols` / `search_semantic`.
3. Find: `search_symbols` (names), `search_semantic` (concepts), `search_text` (literals). Read `summary`/`signature` first; fetch `get_symbol_source` for what you will actually work with.

### For non-trivial changes — close the loop

PureContext is judgment, not actuation: you make the edit; these tools say what's safe and what you forgot. Use them when the change is risky or touches shared code; skip them for trivial edits.

- Pre-edit: `prepare_change` (existing code) or `check_consistency` (new symbols) → risk, forgotten co-change partners (`missingCoChange`), tests, and a `gate`.
- After a write: `index_file({ repoId, filePaths })` — one file, cheap. Prefer it over full `index_folder` mid-task.
- Verify: `verify_change({ repoId, diff, predictedFilePaths, predictedCoChange })`; pre-merge: `merge_readiness`.
- Gate tools return `{ gate: "pass" | "warn" | "block", gateReasons, nextAction }` — `block` means fix first.

### Useful defaults

- Impact before changing a shared symbol: `get_blast_radius`; every call site: `find_references`; risk verdict: `get_symbol_risk`.
- Rename / delete / move pre-flight: `check_rename_safe` / `check_delete_safe` / `check_move_safe`.
- A `verdict: "no_match"` from `search_symbols` means the symbol is not in the index — report the gap instead of retrying many query variants.

Full tool reference: `AGENT_REFERENCE.md` in the project root; harness loop recipes: `docs/HARNESS-CONTRACT.md`.
<!-- purecontext-mcp-end -->
